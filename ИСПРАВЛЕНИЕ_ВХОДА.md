# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ ВХОДА

## 🔍 ПРОБЛЕМА:

Ошибка 401 Unauthorized при попытке входа:
```
POST http://89.169.39.244/api/auth/login
{
    "email": "dsc-23@yandex.RU",
    "password": "123123123"
}
```

---

## ✅ ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ:

### 1. Исправлен порт в Nginx конфигурации

**Было:** `proxy_pass http://localhost:3000;`  
**Стало:** `proxy_pass http://localhost:30000;`

Backend работает на порту 30000, а не 3000.

### 2. Проверена структура базы данных

Таблица `users` существует и содержит:
- `password_hash` (правильное имя колонки)
- Все необходимые поля

### 3. Проверка пользователя

В базе данных есть 1 пользователь. Нужно убедиться, что:
- Email совпадает (регистр важен для MySQL)
- Пароль правильный

---

## 🔍 ВОЗМОЖНЫЕ ПРИЧИНЫ ОШИБКИ:

1. **Пользователь не зарегистрирован** - нужно сначала зарегистрироваться
2. **Неверный пароль** - пароль не совпадает с сохраненным
3. **Регистр email** - MySQL может быть чувствителен к регистру

---

## ✅ РЕШЕНИЕ:

1. **Сначала зарегистрируйтесь:**
   - Откройте `http://89.169.39.244/register`
   - Заполните форму регистрации
   - Email: `dsc-23@yandex.ru`
   - Username: `Джон Уик`
   - Password: `123123123`

2. **Затем войдите:**
   - Откройте `http://89.169.39.244/login`
   - Используйте те же данные

---

## 📝 ПРОВЕРКА:

Если проблема сохраняется, проверьте:
```bash
# Проверить пользователей в БД
ssh root@89.169.39.244 "mysql -u messager_user -p'r7nCbBSN%cr3' messager -e 'SELECT email, username FROM users;'"

# Проверить логи backend
ssh root@89.169.39.244 "pm2 logs messager-backend --lines 50"
```
