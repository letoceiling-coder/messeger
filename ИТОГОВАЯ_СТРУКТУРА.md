# ИТОГОВАЯ СТРУКТУРА ПРОЕКТА НА СЕРВЕРЕ

## 📁 СТРУКТУРА

```
/home/dsc23ytp/
│
├── messager/                          # Рабочая директория
│   ├── backend/                       # Backend исходники
│   │   ├── src/                       # ✅ Синхронизировано
│   │   ├── prisma/                    # ✅ Синхронизировано
│   │   ├── package.json               # ✅ Синхронизировано
│   │   └── ...
│   │
│   ├── frontend-web/                  # Frontend исходники
│   │   ├── src/                       # Синхронизируется
│   │   ├── package.json
│   │   └── ...
│   │
│   └── mobile/                        # Mobile исходники
│
└── parser-auto.site-access.ru/
    └── public_html/                   # Публичная директория
        └── ...                        # Сюда копируется Frontend build
```

---

## 🎯 НАЗНАЧЕНИЕ ДИРЕКТОРИЙ

### `~/messager/` - Рабочая директория

**Для чего:**
- Хранение исходного кода
- Разработка и сборка
- Управление версиями
- **НЕ доступна** напрямую через веб

**Содержит:**
- Backend исходники (NestJS)
- Frontend исходники (React)
- Mobile исходники (React Native)
- Конфигурации

---

### `~/parser-auto.site-access.ru/public_html/` - Публичная директория

**Для чего:**
- Размещение **собранного Frontend**
- Доступ через домен `https://parser-auto.siteaccess.ru/`
- Только статические файлы

**Содержит:**
- `index.html`
- `assets/` (JS, CSS)
- Другие статические файлы

**Как попадают файлы:**
```bash
cd ~/messager/frontend-web
npm run build
cp -r dist/* ~/parser-auto.site-access.ru/public_html/
```

---

## 🔧 КАК РАБОТАЕТ

### Backend

```
~/messager/backend/
  ↓ npm run build
~/messager/backend/dist/
  ↓ pm2 start
Работает на: http://localhost:3000
Доступен через: https://parser-auto.siteaccess.ru/api
```

### Frontend

```
~/messager/frontend-web/
  ↓ npm run build
~/messager/frontend-web/dist/
  ↓ cp -r dist/* public_html/
~/parser-auto.site-access.ru/public_html/
  ↓ Nginx
Доступен через: https://parser-auto.siteaccess.ru/
```

---

## ✅ ПРЕИМУЩЕСТВА

1. **Безопасность** - исходники не доступны
2. **Организация** - все на своих местах
3. **Гибкость** - легко обновлять
4. **Стандарт** - так делают все

---

## 📋 ИТОГО

- **`~/messager/`** = рабочая директория (исходники)
- **`public_html/`** = публичная директория (только build)
- Это правильная структура! ✅
