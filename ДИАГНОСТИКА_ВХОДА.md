# 🔍 ДИАГНОСТИКА ПРОБЛЕМЫ ВХОДА

## 🔍 ПРОБЛЕМА:

При попытке входа через браузер:
- Email: `dsc-23@yandex.RU` или `dsc-23@yandex.ru`
- Password: `123123123`
- Результат: **401 Unauthorized**

---

## ✅ ВЫПОЛНЕННЫЕ ДЕЙСТВИЯ:

### 1. Проверка базы данных
- ✅ Пользователь существует: `dsc-23@yandex.ru`
- ✅ Hash пароля правильной длины: 60 символов (bcrypt)
- ✅ Email нормализован в lowercase

### 2. Добавлено логирование
Добавлены console.log в `auth.service.ts` для диагностики:
- Логирование email при попытке входа
- Логирование результата поиска пользователя
- Логирование результата проверки пароля

### 3. Проверка конфигурации
- ✅ Nginx правильно проксирует `/api` на `localhost:30000`
- ✅ Backend работает на порту 30000
- ✅ Frontend правильно настроен (`.env.production`)

---

## 🔍 СЛЕДУЮЩИЕ ШАГИ:

1. **Попробуйте войти через браузер** с:
   - Email: `dsc-23@yandex.ru` (lowercase)
   - Password: `123123123`

2. **Проверьте логи backend:**
   ```bash
   ssh root@89.169.39.244 "pm2 logs messager-backend --lines 50 | grep AUTH"
   ```

3. **Если пользователь не найден:**
   - Проверьте, что email в базе точно `dsc-23@yandex.ru`
   - Возможно, нужно пересоздать пользователя

4. **Если пароль не совпадает:**
   - Возможно, пароль был захеширован неправильно при регистрации
   - Нужно будет пересоздать пользователя с правильным паролем

---

## 📝 ВОЗМОЖНЫЕ ПРИЧИНЫ:

1. **Пользователь не найден** - проблема с поиском по email
2. **Пароль не совпадает** - проблема с хешированием/проверкой
3. **Проблема с нормализацией email** - регистр не учитывается правильно

---

## ✅ РЕШЕНИЕ:

После проверки логов будет понятно, в чем проблема. Затем исправим соответствующий код.
