# 🚀 НАЧАТЬ СБОРКУ APK - БЫСТРАЯ ИНСТРУКЦИЯ

**Дата:** 31 января 2026  
**Время:** 60-90 минут

---

## 📋 ПЛАН ДЕЙСТВИЙ (8 шагов):

1. ✅ Скачать и установить Java JDK 17
2. ✅ Скачать и установить Android Studio
3. ✅ Установить Android SDK через Android Studio
4. ✅ Настроить переменные окружения
5. ✅ Установить зависимости React Native
6. ✅ Создать keystore для подписи
7. ✅ Собрать APK
8. ✅ Загрузить на сервер

---

## 🔥 ШАГ 1: УСТАНОВКА JAVA JDK (5-10 минут)

### Скачать:

**Ссылка:** https://adoptium.net/temurin/releases/?os=windows&arch=x64&package=jdk&version=17

**Файл:** `OpenJDK17U-jdk_x64_windows_hotspot_xxx.msi` (~180 MB)

### Установить:

1. Запустите скачанный `.msi` файл
2. Нажмите "Next" → "Next" → "Install"
3. Дождитесь завершения
4. Готово!

**Установится в:** `C:\Program Files\Eclipse Adoptium\jdk-17.x.x-hotspot\`

### Проверить:

Откройте **новое** окно PowerShell:

```powershell
java -version
```

Должно показать:
```
openjdk version "17.0.x"
```

**Если не работает:** перезапустите компьютер и проверьте снова.

---

## 🎨 ШАГ 2: УСТАНОВКА ANDROID STUDIO (15-30 минут)

### Скачать:

**Ссылка:** https://developer.android.com/studio

**Файл:** `android-studio-xxx-windows.exe` (~1.1 GB)

**Время скачивания:** 5-15 минут (зависит от интернета)

### Установить:

1. Запустите скачанный `.exe` файл
2. Выберите **"Standard"** installation
3. Согласитесь со всеми компонентами:
   - ✅ Android SDK
   - ✅ Android SDK Platform
   - ✅ Android Virtual Device

4. Дождитесь завершения установки (~3-5 GB скачается)

**SDK установится в:**
```
C:\Users\dsc-2\AppData\Local\Android\Sdk
```

---

## 📦 ШАГ 3: УСТАНОВКА ANDROID SDK КОМПОНЕНТОВ (10-15 минут)

### В Android Studio:

1. Откройте **Android Studio**
2. Нажмите **"More Actions"** → **"SDK Manager"**

### Установить:

#### SDK Platforms (вкладка):

- ✅ **Android 14.0 (API 34)** - рекомендуется
- ✅ **Android 13.0 (API 33)**

#### SDK Tools (вкладка):

- ✅ **Android SDK Build-Tools 34.0.0**
- ✅ **Android SDK Command-line Tools**
- ✅ **Android SDK Platform-Tools**
- ✅ **Android Emulator** (опционально)

3. Нажмите **"Apply"**
4. Дождитесь скачивания (~2-3 GB)

---

## 🌍 ШАГ 4: НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ (5 минут)

### Автоматически (РЕКОМЕНДУЕТСЯ):

1. Откройте **PowerShell от имени Администратора**:
   - Win+X → "Windows PowerShell (Администратор)"

2. Запустите скрипт:

```powershell
cd c:\OSPanel\domains\Messager
.\scripts\setup-android-env.ps1
```

3. Следуйте инструкциям на экране

4. **Перезапустите PowerShell!**

### Проверить:

В **НОВОМ** окне PowerShell:

```powershell
echo $env:ANDROID_HOME
echo $env:JAVA_HOME
java -version
adb version
```

Все должно показать пути и версии!

---

## 📦 ШАГ 5: УСТАНОВКА ЗАВИСИМОСТЕЙ (10-15 минут)

```powershell
cd c:\OSPanel\domains\Messager\mobile
npm install
```

**Время:** 10-15 минут (много нативных модулей)

**Если появляются ошибки:**

```powershell
npm install --legacy-peer-deps
```

**Ожидайте завершения!** Должно показать:

```
added XXX packages in XXs
```

---

## 🔑 ШАГ 6: СОЗДАНИЕ KEYSTORE (2 минуты)

```powershell
cd c:\OSPanel\domains\Messager\mobile\android\app

keytool -genkey -v -keystore messenger-release.keystore -alias messenger-key -keyalg RSA -keysize 2048 -validity 10000
```

**При запросе введите:**

- **Пароль keystore:** `messenger2026`
- **Подтвердите пароль:** `messenger2026`
- **Имя и фамилия:** `Messenger Team`
- **Название организации:** `Messenger`
- **Город:** `Moscow`
- **Область:** `Moscow`
- **Код страны:** `RU`
- **Пароль ключа:** `messenger2026` (Enter для того же пароля)

Должно показать:
```
[Storing messenger-release.keystore]
```

---

## 🏗️ ШАГ 7: СБОРКА APK (5-15 минут)

### Автоматически (РЕКОМЕНДУЕТСЯ):

```powershell
cd c:\OSPanel\domains\Messager
.\scripts\build-apk.ps1
```

**Время первой сборки:** 10-15 минут  
**Последующие сборки:** 3-5 минут

Скрипт:
- Проверит окружение
- Соберёт APK
- Покажет путь к файлу

### Вручную:

```powershell
cd c:\OSPanel\domains\Messager\mobile\android
.\gradlew assembleRelease
```

**APK будет здесь:**
```
mobile\android\app\build\outputs\apk\release\app-release.apk
```

**Размер:** ~20-30 MB

---

## 📤 ШАГ 8: ЗАГРУЗКА НА СЕРВЕР (1 минута)

### Автоматически:

```powershell
cd c:\OSPanel\domains\Messager
.\scripts\build-apk.ps1 -Upload
```

### Вручную:

```powershell
cd c:\OSPanel\domains\Messager

scp mobile\android\app\build\outputs\apk\release\app-release.apk root@89.169.39.244:/var/www/messenger/downloads/messenger-v1.0.0.apk
```

**Пароль:** `r4w*F+jVbD2Z`

### Проверить:

```powershell
ssh root@89.169.39.244 "ls -lh /var/www/messenger/downloads/"
```

Должен показать файл `messenger-v1.0.0.apk`

---

## 🌐 ШАГ 9: ПРОВЕРКА (1 минута)

### На телефоне Android:

1. Откройте браузер
2. Перейдите: **http://89.169.39.244/download.html**
3. Нажмите "Скачать для Android"
4. Разрешите установку из неизвестных источников
5. Установите APK
6. Запустите приложение!

---

## ✅ CHECKLIST:

Отмечайте по мере выполнения:

- [ ] Java JDK 17 установлена (`java -version`)
- [ ] Android Studio установлена
- [ ] Android SDK установлен (API 34)
- [ ] Переменные окружения настроены (`echo $env:ANDROID_HOME`)
- [ ] Зависимости установлены (`npm install` завершён)
- [ ] Keystore создан (`messenger-release.keystore` существует)
- [ ] APK собран (`app-release.apk` создан)
- [ ] APK загружен на сервер
- [ ] APK доступен по ссылке
- [ ] APK протестирован на телефоне

---

## 🐛 TROUBLESHOOTING:

### Ошибка: "SDK location not found"

**Решение:** Создайте файл `mobile/android/local.properties`:

```properties
sdk.dir=C:\\Users\\dsc-2\\AppData\\Local\\Android\\Sdk
```

### Ошибка: "Execution failed for task"

**Решение:**

```powershell
cd mobile\android
.\gradlew clean
.\gradlew assembleRelease
```

### Ошибка: нативные модули не компилируются

**Решение:** Упростите зависимости (см. `SETUP_ANDROID_SDK.md`)

### Ошибка: gradlew не найден

**Решение:**

```powershell
cd mobile\android
gradle wrapper
.\gradlew assembleRelease
```

---

## 📊 ВРЕМЯ НА КАЖДЫЙ ШАГ:

| Шаг | Время | Действие |
|-----|-------|----------|
| 1. Java | 5-10 мин | Скачать + установить |
| 2. Android Studio | 15-30 мин | Скачать + установить |
| 3. SDK Components | 10-15 мин | Скачать через AS |
| 4. Переменные | 5 мин | Запустить скрипт |
| 5. npm install | 10-15 мин | Подождать |
| 6. Keystore | 2 мин | Ввести данные |
| 7. Сборка APK | 10-15 мин | Первая сборка |
| 8. Загрузка | 1 мин | scp на сервер |

**Итого:** 60-90 минут

---

## 💡 БЫСТРЫЕ КОМАНДЫ:

### Полная сборка и загрузка:

```powershell
cd c:\OSPanel\domains\Messager
.\scripts\build-apk.ps1 -Upload
```

### Пересборка после изменений:

```powershell
cd c:\OSPanel\domains\Messager
.\scripts\build-apk.ps1 -Clean -Upload
```

### Проверка окружения:

```powershell
java -version
echo $env:ANDROID_HOME
adb version
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ СБОРКИ:

1. ✅ Протестируйте APK на телефоне
2. ✅ Соберите feedback от пользователей
3. ✅ Исправьте найденные баги
4. 🔧 Настройте Firebase push уведомления
5. 🔧 Оптимизируйте размер APK
6. 🔧 Подготовьте к публикации в Google Play

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ ДОКУМЕНТЫ:

- **`SETUP_ANDROID_SDK.md`** — Полная инструкция с деталями
- **`BUILD_APK_NOW.md`** — Альтернативные методы сборки
- **`APK_STATUS.md`** — Текущий статус проекта

---

## 🎊 ГОТОВО!

После выполнения всех шагов:

**APK будет доступен:**
```
http://89.169.39.244/downloads/messenger-v1.0.0.apk
```

**Страница скачивания:**
```
http://89.169.39.244/download.html
```

**Приступайте! 🚀**
