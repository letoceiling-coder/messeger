# 🚀 УСТАНОВКА ПО НА VPS - ПОШАГОВО

## ✅ ЧТО УЖЕ СДЕЛАНО

- ✅ Подключение к VPS установлено
- ✅ Система обновлена

**Пароль:** `r4w*F+jVbD2Z`

---

## 📋 КОМАНДЫ ДЛЯ УСТАНОВКИ

Выполните команды по порядку в SSH сессии:

### 1. Установка Node.js 20.x

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
```

### 2. Проверка Node.js

```bash
node --version
npm --version
```

### 3. Установка MySQL

```bash
apt-get install -y mysql-server
systemctl start mysql
systemctl enable mysql
```

### 4. Установка Nginx

```bash
apt-get install -y nginx
systemctl start nginx
systemctl enable nginx
```

### 5. Установка PM2

```bash
npm install -g pm2
```

### 6. Установка дополнительных утилит

```bash
apt-get install -y git curl wget build-essential
```

### 7. Создание директорий

```bash
mkdir -p /var/www/messager/backend
mkdir -p /var/www/messager/frontend-web
mkdir -p /var/www/messager/backend/uploads
mkdir -p /var/www/messager/backend/logs
```

---

## ✅ ПРОВЕРКА УСТАНОВКИ

После выполнения всех команд проверьте:

```bash
node --version
npm --version
mysql --version
nginx -v
pm2 --version
```

---

## 🚀 ГОТОВО!

После установки сообщите мне, и я помогу:
1. Настроить MySQL
2. Перенести проект на VPS
3. Настроить Nginx
4. Запустить Backend
