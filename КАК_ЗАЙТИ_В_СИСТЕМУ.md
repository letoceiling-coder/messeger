# 🔐 КАК ЗАЙТИ В СИСТЕМУ

## 📍 ТЕКУЩАЯ СИТУАЦИЯ

Сайт `https://parser-auto.siteaccess.ru/` редиректит на `/login` - это **нормально**! Это означает, что:
- ✅ Frontend работает
- ✅ Роутинг настроен правильно
- ✅ Нужно зарегистрироваться или войти

---

## 🚀 КАК ЗАЙТИ

### Вариант 1: Регистрация через интерфейс

1. Откройте: `https://parser-auto.siteaccess.ru/`
2. Вы автоматически попадете на страницу `/login`
3. Нажмите кнопку **"Регистрация"** или перейдите на `/register`
4. Заполните форму:
   - **Email:** `test@example.com`
   - **Username:** `testuser`
   - **Password:** `password123`
5. Нажмите **"Зарегистрироваться"**
6. После регистрации вы автоматически войдете в систему

### Вариант 2: Создать пользователя через API

Если интерфейс регистрации не работает, создайте пользователя через API:

```bash
curl -X POST https://parser-auto.siteaccess.ru/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

Затем войдите через интерфейс с этими данными.

### Вариант 3: Через SSH (если API не доступен)

```bash
ssh dsc23ytp@5.101.156.207

# Проверить, работает ли Backend
curl http://localhost:3000/health

# Создать пользователя через API напрямую
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

---

## ⚠️ ЧТО ОСТАЛОСЬ СДЕЛАТЬ

### 1. Настроить Nginx для API (ВАЖНО!)

**Проблема:** API не доступен через `https://parser-auto.siteaccess.ru/api/`

**Решение:** Настроить Nginx через панель Beget (см. `НАСТРОЙКА_NGINX_БЕГЕТ.md`)

**Без этого:**
- ❌ Регистрация не будет работать
- ❌ Логин не будет работать
- ❌ Чат не будет работать

### 2. Проверить работу

После настройки Nginx:

1. ✅ Откройте `https://parser-auto.siteaccess.ru/`
2. ✅ Зарегистрируйтесь
3. ✅ Войдите в систему
4. ✅ Проверьте работу чата

---

## 🔧 БЫСТРАЯ ПРОВЕРКА

### Проверить Backend:

```bash
ssh dsc23ytp@5.101.156.207
curl http://localhost:3000/health
```

Должен вернуть: `{"status":"ok"}`

### Проверить Frontend:

Откройте: `https://parser-auto.siteaccess.ru/`

Должна открыться страница логина.

### Проверить API через Nginx:

```bash
curl https://parser-auto.siteaccess.ru/api/health
```

Если возвращает 404 - нужно настроить Nginx!

---

## ✅ ИТОГО

1. **Сейчас:** Frontend работает, редирект на `/login` - это нормально
2. **Нужно:** Настроить Nginx для проксирования `/api` → `localhost:3000`
3. **После:** Можно регистрироваться и использовать мессенджер

**Следующий шаг:** Настроить Nginx через панель Beget! 📋
