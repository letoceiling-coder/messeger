# Добавленный недостающий функционал в план

Этот документ фиксирует функционал, который был добавлен в **PLAN_MESSENGER_IMPROVEMENTS.md** для достижения полноты современного мессенджера.

---

## Список добавленных функций

### 1. Редактирование сообщений (п. 13, Этап 15)
- **Что:** Возможность отредактировать отправленное текстовое сообщение.
- **UI:** Долгое нажатие → «Редактировать», пометка «изменено» рядом с текстом.
- **Backend:** API PATCH `/messages/:id`, поле `isEdited` уже есть в схеме.
- **WebSocket:** Событие `message:edited` для обновления у других участников.

### 2. Групповые чаты (п. 14, Этап 16)
- **Что:** Создание групповых чатов (не только 1-на-1), управление участниками, роли администраторов.
- **UI:** Создание группы, экран информации о группе, добавление/удаление участников, название и аватар группы.
- **Backend:** Тип чата `"group"`, роли `admin`/`member` уже есть в схеме, новые API эндпоинты.
- **WebSocket:** События при изменении состава или ролей группы.

### 3. Поиск по сообщениям (п. 15, Этап 17)
- **Что:** Поиск по содержимому сообщений внутри чата и глобальный поиск по всем чатам.
- **UI:** Кнопка поиска в шапке чата, поле глобального поиска в списке чатов.
- **Backend:** API `GET /chats/:id/messages/search` и `GET /messages/search` с параметром `q`.

### 4. Реакции на сообщения (п. 16, Этап 18)
- **Что:** Быстрые эмодзи-реакции (👍 ❤️ 😂 и т.д.) на сообщения.
- **UI:** Панель эмодзи под сообщением, отображение счётчика реакций, список пользователей при клике.
- **Backend:** Таблица `message_reactions`, API POST/DELETE, при получении сообщений отдавать реакции.
- **WebSocket:** Событие `message:reaction` для обновления в реальном времени.

### 5. Статус "печатает..." (п. 17, Этап 19)
- **Что:** Индикатор, что собеседник набирает текст.
- **UI:** Подпись «[Имя] печатает...» под шапкой чата или в нижней панели.
- **Backend (WebSocket):** События `typing:start`, `typing:stop`, рассылка `user:typing` участникам.
- **Frontend:** Отслеживание ввода в поле, отправка событий, отображение индикатора.

### 6. Прикрепление документов (п. 18, Этап 20)
- **Что:** Прикрепление и отправка любых файлов (PDF, DOC, ZIP и т.д.), не только фото/видео/аудио.
- **UI:** Опция «Документ» при выборе вложений, превью и отображение в чате (иконка типа, название, размер, кнопка «Скачать»).
- **Backend:** Расширение эндпоинта загрузки, тип сообщения `"document"`, поля `fileName`, `fileSize`, `mimeType`.

### 7. Черновики сообщений (п. 19, Этап 21.1)
- **Что:** Сохранение несохранённого текста при переходе между чатами.
- **UI:** При возврате в чат текст восстанавливается; индикатор черновика в списке чатов.
- **Реализация:** localStorage или контекст на клиенте.

### 8. Закрепленные сообщения (п. 20, Этап 21.2)
- **Что:** Закрепление важных сообщений вверху чата.
- **UI:** Контекстное меню → «Закрепить», отображение закреплённого сообщения под шапкой чата с возможностью прокрутки к нему.
- **Backend:** Таблица `pinned_messages` или поле `pinnedMessageId` в `Chat`, API POST/DELETE.

### 9. Темная/светлая тема (п. 21, Этап 21.3)
- **Что:** Переключение между светлой, тёмной и системной темой.
- **UI:** Переключатель в настройках, применение темы без перезагрузки.
- **Реализация:** Два набора токенов цветов (CSS/Tailwind), сохранение выбора в localStorage.

### 10. Архивация чатов (п. 22, Этап 21.4)
- **Что:** Скрытие неактивных чатов в архив с возможностью возврата.
- **UI:** Свайп или долгое нажатие → «Архивировать», отдельная вкладка «Архив».
- **Backend:** Поле `isArchived` в `chat_members` (у каждого пользователя свой флаг).

### 11. Блокировка пользователей (п. 23, Этап 21.5)
- **Что:** Блокировка нежелательных пользователей (заблокированный не может писать, не видит онлайн).
- **UI:** Кнопка «Заблокировать» в профиле пользователя, блокированные сообщения не показываются.
- **Backend:** Таблица `blocked_users`, API POST/DELETE, проверка при отправке сообщений.

---

## Приоритет реализации

- **Высокий (рекомендуется после базовых этапов 0–13):**
  - Редактирование сообщений (Этап 15)
  - Групповые чаты (Этап 16) — важнейшая функция
  - Поиск по сообщениям (Этап 17)
  - Статус "печатает..." (Этап 19)

- **Средний:**
  - Реакции на сообщения (Этап 18)
  - Прикрепление документов (Этап 20)
  - Темная/светлая тема (Этап 21.3)

- **Низкий (по желанию):**
  - Черновики (Этап 21.1)
  - Закрепленные сообщения (Этап 21.2)
  - Архивация чатов (Этап 21.4)
  - Блокировка пользователей (Этап 21.5)

---

## Связь с основным планом

Все добавленные функции интегрированы в **PLAN_MESSENGER_IMPROVEMENTS.md**:
- В сводку «что есть / что нужно» добавлены пункты 13–23.
- В порядок реализации добавлены этапы 15–21.
- Детальное описание каждого этапа (задачи, зависимости, файлы) добавлено перед этапом 14 (единый формат).

Порядок выполнения после базовых этапов (0–13):
1. Этап 15 — редактирование
2. Этап 16 — групповые чаты (важно!)
3. Этап 17 — поиск
4. Этап 18 — реакции
5. Этап 19 — статус "печатает..."
6. Этап 20 — документы
7. Этап 21 — дополнительный функционал (черновики, закрепление, темы, архивация, блокировка)
8. Этап 14 — обязательный этап: единый формат веб и мобильных приложений.
