# 🚀 Руководство по настройке и запуску

## 📋 Предварительные требования

### Для всех платформ:
- **Node.js** 18+ ([скачать](https://nodejs.org/))
- **npm** или **yarn**
- **Git**

### Для Android:
- **JDK 17** ([скачать](https://www.oracle.com/java/technologies/downloads/#java17))
- **Android Studio** ([скачать](https://developer.android.com/studio))
- **Android SDK** (API 33+)

### Для iOS (только macOS):
- **Xcode** 14+ (из App Store)
- **CocoaPods** (`sudo gem install cocoapods`)
- **Xcode Command Line Tools** (`xcode-select --install`)

---

## 🛠️ Установка

### 1. Клонировать репозиторий (если ещё не сделано)

```bash
git clone <your-repo-url>
cd Messager/mobile
```

### 2. Установить зависимости

```bash
npm install
```

### 3. Для iOS (только macOS):

```bash
cd ios
pod install
cd ..
```

---

## ⚙️ Конфигурация

### 1. Настроить API

Откройте `src/config/api.ts` и укажите адрес вашего сервера:

```typescript
export const API_BASE_URL = 'http://YOUR_SERVER_IP:3001';
export const WS_BASE_URL = 'ws://YOUR_SERVER_IP:3001';
```

**Для локальной разработки:**

- **Android эмулятор:** `http://10.0.2.2:3001`
- **iOS симулятор:** `http://localhost:3001`
- **Реальное устройство:** `http://192.168.X.X:3001` (ваш локальный IP)

**Как узнать свой локальный IP:**

```bash
# Windows
ipconfig

# macOS/Linux
ifconfig | grep inet
```

### 2. Firebase настройка (для Push уведомлений)

#### Создать проект Firebase:

1. Перейти на [Firebase Console](https://console.firebase.google.com/)
2. Создать новый проект
3. Добавить Android и/или iOS приложение

#### Android:

1. В Firebase Console → Project Settings → Add app → Android
2. Указать package name: `com.messengermobile` (или ваш)
3. Скачать `google-services.json`
4. Поместить файл в `android/app/`

#### iOS:

1. В Firebase Console → Project Settings → Add app → iOS
2. Указать bundle ID: `com.messengermobile` (или ваш)
3. Скачать `GoogleService-Info.plist`
4. Открыть `ios/MessengerMobile.xcworkspace` в Xcode
5. Перетащить `GoogleService-Info.plist` в проект (в Xcode)

---

## 🏃 Запуск приложения

### Запустить Metro Bundler:

```bash
npm start
```

### Android:

#### Способ 1: Используя команду

```bash
npm run android
```

#### Способ 2: Через Android Studio

1. Открыть `android/` в Android Studio
2. Дождаться синхронизации Gradle
3. Выбрать эмулятор или устройство
4. Нажать **Run** (▶️)

#### Если эмулятора нет:

1. В Android Studio: **Tools → AVD Manager**
2. **Create Virtual Device**
3. Выбрать устройство (например, Pixel 4)
4. Выбрать образ системы (API 33+)
5. **Finish**

### iOS (только macOS):

#### Способ 1: Используя команду

```bash
npm run ios
```

#### Способ 2: Через Xcode

1. Открыть `ios/MessengerMobile.xcworkspace` в Xcode
2. Выбрать симулятор или устройство
3. Нажать **Run** (▶️)

---

## 🐛 Решение проблем

### Проблема: "Unable to resolve module"

**Решение:**

```bash
npm start -- --reset-cache
```

### Проблема: Android build failed

**Решение:**

```bash
cd android
./gradlew clean
cd ..
npm run android
```

### Проблема: iOS build failed

**Решение:**

```bash
cd ios
pod deintegrate
pod install
cd ..
npm run ios
```

### Проблема: "Failed to connect to /10.0.2.2:3001"

**Решение:**

Убедитесь, что:
1. Backend сервер запущен
2. В `src/config/api.ts` указан правильный адрес
3. Firewall не блокирует порт 3001

### Проблема: Белый экран при запуске

**Решение:**

1. Проверить логи: `npx react-native log-android` или `npx react-native log-ios`
2. Очистить кэш: `npm start -- --reset-cache`
3. Пересобрать: `npm run android` или `npm run ios`

---

## 📱 Установка на реальное устройство

### Android:

#### Через USB:

1. Включить **Developer Options** на устройстве:
   - **Settings → About phone → Tap "Build number" 7 times**
2. Включить **USB Debugging**:
   - **Settings → Developer options → USB debugging**
3. Подключить устройство через USB
4. Разрешить отладку на устройстве
5. Запустить: `npm run android`

#### Через WiFi (ADB Wireless):

```bash
# Найти IP устройства (Settings → About → Status → IP address)
adb tcpip 5555
adb connect <DEVICE_IP>:5555
npm run android
```

### iOS:

1. Подключить устройство через USB
2. В Xcode: **Product → Destination → Select your device**
3. **Product → Run** (Cmd + R)
4. При первом запуске: **Settings → General → Device Management → Trust developer**

---

## 🔧 Полезные команды

### Очистка кэша:

```bash
npm start -- --reset-cache
```

### Логи:

```bash
# Android
npx react-native log-android

# iOS
npx react-native log-ios
```

### Проверить подключенные устройства:

```bash
# Android
adb devices

# iOS
xcrun simctl list devices
```

### Перезапустить Metro:

```bash
# Остановить (Ctrl+C), затем:
npm start
```

---

## 📦 Сборка для production

### Android APK:

```bash
cd android
./gradlew assembleRelease
```

APK будет в: `android/app/build/outputs/apk/release/app-release.apk`

### Android AAB (для Google Play):

```bash
cd android
./gradlew bundleRelease
```

AAB будет в: `android/app/build/outputs/bundle/release/app-release.aab`

### iOS:

1. Открыть `ios/MessengerMobile.xcworkspace` в Xcode
2. Выбрать **Any iOS Device (arm64)**
3. **Product → Archive**
4. **Distribute App → Ad Hoc / App Store**

---

## 🎨 Визуальная структура приложения

```
┌─────────────────────────────┐
│  Login / Register           │ ← Auth Screens
└─────────────────────────────┘
              ↓
┌─────────────────────────────┐
│  Main Navigator             │
│  ┌───────────┬───────────┐  │
│  │  Chats    │ Settings  │  │ ← Bottom Tabs
│  └───────────┴───────────┘  │
└─────────────────────────────┘
              ↓
┌─────────────────────────────┐
│  Chat Screen                │ ← Messages
└─────────────────────────────┘
              ↓
┌─────────────────────────────┐
│  Call Screen                │ ← Audio/Video calls
└─────────────────────────────┘
```

---

## ✅ Чеклист первого запуска

- [ ] Node.js 18+ установлен
- [ ] JDK 17 установлен (для Android)
- [ ] Android Studio / Xcode установлен
- [ ] Зависимости установлены (`npm install`)
- [ ] iOS pods установлены (`cd ios && pod install`)
- [ ] `src/config/api.ts` настроен (IP сервера)
- [ ] Firebase проект создан
- [ ] `google-services.json` добавлен (Android)
- [ ] `GoogleService-Info.plist` добавлен (iOS)
- [ ] Backend сервер запущен
- [ ] Metro bundler запущен (`npm start`)
- [ ] Приложение запущено (`npm run android` / `npm run ios`)

---

## 🎯 Следующие шаги

После успешного запуска:

1. **Протестировать функционал:**
   - Регистрация/вход
   - Отправка сообщений
   - Черновики
   - Темы

2. **Настроить Push уведомления:**
   - Получить FCM Server Key
   - Настроить backend для отправки

3. **Добавить функции:**
   - Голосовые сообщения
   - Фото/видео
   - Документы
   - Звонки (WebRTC)

4. **Оптимизация:**
   - Уменьшить размер APK
   - Улучшить производительность
   - Добавить аналитику

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте [Issues](../issues)
2. Посмотрите логи (`npx react-native log-android/ios`)
3. Создайте новый issue с описанием проблемы и логами

---

**Удачи в разработке! 🚀**
