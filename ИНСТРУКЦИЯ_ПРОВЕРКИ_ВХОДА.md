# 🔍 ИНСТРУКЦИЯ ПО ПРОВЕРКЕ ВХОДА

## ✅ ЧТО СДЕЛАНО:

1. ✅ Добавлено логирование в `auth.service.ts`
2. ✅ Обновлена конфигурация Nginx (добавлен IP в server_name)
3. ✅ Backend перезапущен с новым кодом

---

## 🔍 КАК ПРОВЕРИТЬ:

### 1. Попробуйте войти через браузер:
- Откройте: `http://89.169.39.244/login`
- Email: `dsc-23@yandex.ru` (используйте lowercase)
- Password: `123123123`

### 2. Проверьте логи backend:
```bash
ssh root@89.169.39.244 "pm2 logs messager-backend --lines 50 | grep AUTH"
```

Вы должны увидеть логи вида:
```
[AUTH] Login attempt: { email: '...', normalized: '...' }
[AUTH] User found: dsc-23@yandex.ru
[AUTH] Comparing password...
[AUTH] Password valid: true/false
```

### 3. Если пользователь не найден:
- Проверьте email в базе: `dsc-23@yandex.ru`
- Возможно, нужно пересоздать пользователя

### 4. Если пароль не совпадает:
- Пароль мог быть захеширован неправильно при регистрации
- Нужно будет пересоздать пользователя

---

## 📝 ВОЗМОЖНЫЕ РЕШЕНИЯ:

### Если пользователь не найден:
1. Удалите пользователя из базы
2. Зарегистрируйтесь заново через `/register`

### Если пароль не совпадает:
1. Удалите пользователя из базы
2. Зарегистрируйтесь заново с тем же паролем

---

## 🔧 КОМАНДЫ ДЛЯ ПРОВЕРКИ:

```bash
# Проверить пользователей в базе
ssh root@89.169.39.244 "mysql -u messager_user -pr7nCbBSN%cr3 messager -e 'SELECT email, username FROM users;'"

# Проверить логи в реальном времени
ssh root@89.169.39.244 "pm2 logs messager-backend --lines 0"

# Удалить пользователя (если нужно)
ssh root@89.169.39.244 "mysql -u messager_user -pr7nCbBSN%cr3 messager -e \"DELETE FROM users WHERE email = 'dsc-23@yandex.ru';\""
```

---

## ✅ СЛЕДУЮЩИЕ ШАГИ:

1. Попробуйте войти через браузер
2. Проверьте логи
3. Сообщите, что показывают логи
4. Исправлю проблему на основе логов
