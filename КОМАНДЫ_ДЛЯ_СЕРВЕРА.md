# КОМАНДЫ ДЛЯ ВЫПОЛНЕНИЯ НА СЕРВЕРЕ BEGET

## 🔑 ШАГ 1: НАСТРОЙКА SSH КЛЮЧЕЙ

### На вашем компьютере (Windows)

1. **Запустите скрипт настройки:**
   ```powershell
   .\setup-ssh-simple.ps1
   ```

2. **Скопируйте публичный ключ**, который покажет скрипт

### На сервере Beget

Выполните эти команды на сервере (через SSH или панель управления):

```bash
# Создать директорию .ssh
mkdir -p ~/.ssh

# Добавить ваш публичный ключ (вставьте ключ из скрипта)
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ_ЗДЕСЬ" >> ~/.ssh/authorized_keys

# Установить правильные права
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

**Где взять публичный ключ?**
- Запустите `.\setup-ssh-simple.ps1` на Windows
- Или выполните: `type $env:USERPROFILE\.ssh\id_rsa.pub` в PowerShell

---

## 📁 ШАГ 2: СОЗДАНИЕ СТРУКТУРЫ ПРОЕКТА

Выполните на сервере:

```bash
# Создать структуру директорий
mkdir -p ~/messager/{backend,frontend-web,mobile,nginx}

# Проверить
ls -la ~/messager/
```

---

## 🔧 ШАГ 3: ПРОВЕРКА ОКРУЖЕНИЯ

Проверьте на сервере:

```bash
# Node.js
node --version
npm --version

# PostgreSQL (если нужно)
psql --version

# Redis (если нужно)
redis-cli --version
```

---

## 📦 ШАГ 4: ПОДГОТОВКА К СИНХРОНИЗАЦИИ

После выполнения команд выше, на вашем компьютере:

```powershell
# Синхронизация файлов
.\scripts\sync-to-server.ps1
```

---

## ✅ ПРОВЕРКА

После синхронизации проверьте на сервере:

```bash
# Проверить структуру
ls -la ~/messager/

# Проверить файлы
ls -la ~/messager/backend/
ls -la ~/messager/frontend-web/
```

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Не подключается SSH

1. Проверьте адрес сервера в SSH config
2. Проверьте, что ключ скопирован на сервер
3. Проверьте права на файлы: `chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys`

### Не создается структура

Выполните команды по отдельности:
```bash
mkdir -p ~/messager/backend
mkdir -p ~/messager/frontend-web
mkdir -p ~/messager/mobile
mkdir -p ~/messager/nginx
```

---

## 📝 БЫСТРАЯ СПРАВКА

**Ваш сервер:**
- Пользователь: `dsc23ytp`
- Путь проекта: `~/messager`
- Public HTML: `~/parser-auto.site-access.ru/public_html`

**Подключение:**
```bash
ssh dsc23ytp@ВАШ_АДРЕС_СЕРВЕРА
# или после настройки config:
ssh dragon
```
