# ОБЪЯСНЕНИЕ СТРУКТУРЫ РАЗМЕЩЕНИЯ

## ❓ ПОЧЕМУ `~/messager/` А НЕ `public_html/`?

### Краткий ответ:

**`~/messager/`** - это **рабочая директория** для исходного кода и разработки.

**`public_html/`** - это **публичная директория** только для **собранного Frontend**.

---

## 📁 СТРУКТУРА НА СЕРВЕРЕ

```
/home/dsc23ytp/
│
├── messager/                          # ← РАБОЧАЯ ДИРЕКТОРИЯ
│   ├── backend/                       # Backend исходники
│   │   ├── src/                       # Исходный код
│   │   ├── package.json
│   │   └── ...
│   │
│   ├── frontend-web/                  # Frontend исходники
│   │   ├── src/                       # Исходный код React
│   │   ├── package.json
│   │   └── ...
│   │
│   └── mobile/                        # Mobile исходники
│
└── parser-auto.site-access.ru/
    └── public_html/                   # ← ПУБЛИЧНАЯ ДИРЕКТОРИЯ
        ├── index.html                  # Собранный Frontend
        ├── assets/
        └── ...
```

---

## 🎯 ЗАЧЕМ ТАК?

### 1. Backend (`~/messager/backend/`)

**Backend - это НЕ веб-файлы!**

- Backend - это **Node.js приложение** (сервер)
- Работает на **порту 3000** как отдельный процесс
- **НЕ должен быть** в `public_html/`
- Обрабатывается через **PM2** или **systemd**

**Процесс:**
```bash
cd ~/messager/backend
npm install --production
npm run build          # Собирает в dist/
pm2 start ecosystem.config.js  # Запускает на порту 3000
```

**Доступ:**
- Внутренний: `http://localhost:3000`
- Внешний: `https://parser-auto.siteaccess.ru/api` (через Nginx)

---

### 2. Frontend (`~/messager/frontend-web/` → `public_html/`)

**Исходники** (`~/messager/frontend-web/`):
- Исходный код React
- Нужен для **сборки**
- **НЕ доступен** напрямую через веб

**Build** (`public_html/`):
- **Собранные файлы** после `npm run build`
- Статические файлы (HTML, CSS, JS)
- Доступны **напрямую** через веб-сервер
- Домен указывает на эту папку

**Процесс:**
```bash
cd ~/messager/frontend-web
npm install
npm run build          # Собирает в dist/
cp -r dist/* ~/parser-auto.site-access.ru/public_html/  # Копирует
```

**Доступ:**
- `https://parser-auto.siteaccess.ru/` → показывает `public_html/`

---

## 🌐 КАК ЭТО РАБОТАЕТ ВМЕСТЕ

### Nginx конфигурация:

```nginx
# Frontend (статические файлы из public_html)
https://parser-auto.siteaccess.ru/
  → /home/dsc23ytp/parser-auto.site-access.ru/public_html/

# Backend API (прокси на порт 3000)
https://parser-auto.siteaccess.ru/api
  → http://localhost:3000/api

# WebSocket
https://parser-auto.siteaccess.ru/socket.io/
  → http://localhost:3000/socket.io/
```

---

## ✅ ПРЕИМУЩЕСТВА

1. **Безопасность:**
   - Исходники не доступны через веб
   - Только собранные файлы в public_html

2. **Организация:**
   - Исходники отдельно от build
   - Легко обновлять

3. **Стандартная практика:**
   - Так делают все современные приложения

---

## 📋 ИТОГО

- **`~/messager/`** - для разработки и сборки (исходники)
- **`public_html/`** - для публичного доступа (только build Frontend)
- **Backend** - работает как сервис на порту 3000
- **Frontend** - статические файлы в public_html

Это правильная структура! ✅
