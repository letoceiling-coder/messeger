# 🗑️ ФАЙЛЫ ДЛЯ УДАЛЕНИЯ ПЕРЕД КОПИРОВАНИЕМ

## ❌ УДАЛИТЕ ЭТИ ПАПКИ/ФАЙЛЫ (они не нужны на сервере):

### В корне проекта:
- `mobile/` - мобильное приложение (не нужно на сервере)
- `nginx/` - конфиги уже настроены на сервере
- `scripts/` - скрипты для разработки (опционально, можно оставить)
- Все `.md` файлы в корне (документация) - можно оставить только `README.md`
- `backend.tar.gz` - архив (временный файл)

### В backend/:
- `node_modules/` - будут установлены через `npm install`
- `dist/` - будет собран через `npm run build`
- `.env` (если есть, кроме `.env.production`)
- `logs/` - логи
- `coverage/` - отчеты покрытия

### В frontend-web/:
- `node_modules/` - будут установлены через `npm install`
- `dist/` - будет собран через `npm run build` (если копируете исходники)

---

## ✅ ЧТО КОПИРОВАТЬ:

### Обязательно:
- `backend/` (без node_modules, dist, logs)
- `frontend-web/` (без node_modules, dist) ИЛИ `frontend-web/dist/` (если уже собран)

### Опционально:
- `README.md` - для справки
- `scripts/` - если нужны скрипты на сервере

---

## 🚀 БЫСТРЫЙ СПОСОБ:

**В WinSCP настройте исключения:**
1. Settings → Preferences → Transfer → Exclude
2. Добавьте:
   ```
   node_modules
   .git
   dist
   logs
   coverage
   .vscode
   .idea
   mobile
   *.md
   !README.md
   ```

3. Скопируйте весь проект - WinSCP автоматически исключит ненужное!
