# Описание сущностей и типов данных

Документ описывает все сущности, типы и контексты приложения. Данные должны быть динамическими: загрузка из API, состояние в контекстах, локальное хранилище. Статические данные перечислены в отчёте `STATIC_DATA_REPORT.md`.

**Последнее обновление:** синхронизировано с `src/types/messenger.ts` и `src/types/feed.ts` (Chat.subscribersCount, User.email/dateOfBirth/profileVideoUrl, FeedStory.viewed).

---

## 1. Типы сообщений и чата (`src/types/messenger.ts`)

### MessageStatus
**Тип:** `'sending' | 'sent' | 'delivered' | 'read'`

Статус доставки сообщения: отправляется, отправлено, доставлено, прочитано.

---

### MessageType
**Тип:** объединение строк

Тип контента сообщения:
- `text` — текстовое
- `voice` — голосовое
- `video_note` — видеокружок
- `image` — изображение
- `video` — видео
- `gif` — GIF
- `sticker` — стикер
- `file` — файл
- `poll` — опрос
- `location` — геолокация
- `contact` — контакт
- `system` — системное (например «Звонок завершён»)

---

### Message
**Интерфейс.** Сообщение в чате.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный идентификатор |
| chatId | string | ID чата |
| senderId | string | ID отправителя (user, contact, bot, channel) |
| type | MessageType | Тип сообщения |
| content | string | Текст или подпись к медиа |
| timestamp | Date | Время отправки |
| status | MessageStatus | Статус доставки |
| isOutgoing | boolean | Исходящее от текущего пользователя |
| duration | number? | Длительность голосового (сек) |
| waveform | number[]? | Волновая форма голосового (30–60 баров) |
| isPlayed | boolean? | Голосовое проиграно |
| videoNoteDuration | number? | Длительность видеокружка (сек) |
| mediaUrl | string? | URL медиа |
| thumbnailUrl | string? | URL превью |
| fileName | string? | Имя файла |
| fileSize | number? | Размер файла (байты) |
| contactName | string? | Имя контакта (type === 'contact') |
| contactPhone | string? | Телефон контакта |
| contactAvatar | string? | Аватар контакта |
| stickerId | string? | ID стикера |
| stickerPackId | string? | ID пака стикеров |
| isAnimated | boolean? | Анимированный стикер |
| replyTo | string? | ID сообщения, на которое отвечаем |
| editedAt | Date? | Время последнего редактирования |
| isForwarded | boolean? | Переслано |
| forwardSenderName | string? | Имя отправителя при пересылке |
| views | number? | Просмотры (канал/пост) |
| reactions | MessageReaction[]? | Реакции (эмодзи + счётчик + userIds) |
| buttons | BotButton[]? | Кнопки бота (inline/reply/url) |
| isProcessing | boolean? | Показ «обрабатывается...» у бота |

---

### BotButton
**Интерфейс.** Кнопка в сообщении бота.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string? | Идентификатор |
| type | 'inline' \| 'reply' \| 'url' | inline — в сообщении, reply — ответ выбором, url — переход по ссылке |
| label | string | Текст кнопки |
| action | string? | callback_data (inline) или текст ответа (reply) |
| url | string? | URL для type === 'url' |

---

### BotKeyboardRow
**Тип:** `{ label: string; action?: string }[]`

Строка кнопок кастомной клавиатуры бота (под полем ввода). Фон кнопок #25D366, 14px.

---

### MessageReaction
**Интерфейс.** Реакция на сообщение (канал/пост).

| Поле | Тип | Описание |
|------|-----|----------|
| emoji | string | Эмодзи (например 👍, 👎) |
| count | number | Количество поставивших |
| userIds | string[]? | ID пользователей (для подсветки «вы») |

---

## 2. Стикеры

### StickerStateType
**Тип:** `'idle' | 'panel_open' | 'browsing' | 'searching' | 'sending' | 'receiving' | 'playing'`

Состояние панели/воспроизведения стикеров.

---

### Sticker
**Интерфейс.** Один стикер.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| packId | string | ID пака |
| url | string | URL изображения |
| animatedUrl | string? | URL анимированной версии |
| emoji | string? | Эмодзи-тег |
| keywords | string[]? | Ключевые слова для поиска |

---

### StickerPack
**Интерфейс.** Пак стикеров.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| title | string | Название |
| iconUrl | string | URL иконки пака |
| stickers | Sticker[] | Список стикеров |

---

## 3. Чаты и контакты

### Chat
**Интерфейс.** Чат (диалог, группа, бот, канал).

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| name | string | Название / имя контакта |
| avatar | string? | URL аватара |
| username | string? | @username для ботов и каналов |
| isGroup | boolean | Групповой чат |
| isBot | boolean? | Чат с ботом |
| isChannel | boolean? | Канал (только посты от канала) |
| subscribersCount | number? | Канал: количество подписчиков (для header) |
| keyboard | BotKeyboardRow[]? | Клавиатура бота |
| lastMessage | Message? | Последнее сообщение для превью |
| unreadCount | number | Количество непрочитанных |
| isPinned | boolean | Закреплён |
| isMuted | boolean | Уведомления отключены |
| isArchived | boolean | В архиве |
| members | string[]? | ID участников (группа) |
| isOnline | boolean? | Онлайн (личный чат) |
| lastSeen | Date? | Время последней активности |
| isTyping | boolean? | Печатает |

---

### Contact
**Интерфейс.** Контакт (человек в списке контактов).

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| name | string | Имя |
| username | string? | @username |
| avatar | string? | URL аватара |
| phone | string? | Телефон |
| bio | string? | О себе |
| isOnline | boolean | Онлайн |
| lastSeen | Date? | Последняя активность |
| isTyping | boolean? | Печатает |
| isPinned | boolean? | Закреплён в списке |
| isArchived | boolean? | В архиве |
| isMuted | boolean? | Уведомления отключены |
| isBlocked | boolean? | Заблокирован |

---

## 4. Звонки

### CallType
**Тип:** `'audio' | 'video'`

Тип звонка: аудио или видео.

---

### CallStatus
**Тип:** `'incoming' | 'outgoing' | 'missed' | 'declined'`

Статус в истории: входящий, исходящий, пропущенный, отклонённый.

---

### Call
**Интерфейс.** Запись в истории звонков.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| contactId | string | ID контакта |
| contact | Contact | Контакт (вложенный) |
| type | CallType | Аудио/видео |
| status | CallStatus | Статус |
| timestamp | Date | Время |
| duration | number? | Длительность (сек), если состоялся |

---

### CallSessionState
**Тип:** `'calling' | 'ringing' | 'connected' | 'reconnecting' | 'ended'`

Состояние активного звонка: набор, звонок, разговор, переподключение, завершён.

---

### CallNetworkState
**Тип:** `'good' | 'poor' | 'reconnecting' | 'lost'`

Состояние сети во время звонка.

---

### CallSession
**Интерфейс.** Активная сессия звонка (текущий звонок).

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | ID сессии |
| direction | 'incoming' \| 'outgoing' | Направление |
| type | CallType | Аудио/видео |
| contact | Contact | С кем звонок |
| state | CallSessionState | Состояние |
| networkState | CallNetworkState | Сеть |
| startTime | number \| null | Время соединения (для таймера) |
| muted | boolean | Микрофон выключен |
| speaker | boolean | Громкая связь |
| cameraOff | boolean | Камера выключена (видео) |
| frontCamera | boolean | Передняя камера (видео) |

---

## 5. Пользователь и настройки

### User
**Интерфейс.** Текущий пользователь приложения.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| name | string | Имя |
| username | string | @username |
| avatar | string? | URL аватара |
| phone | string | Телефон |
| email | string? | Эл. почта |
| dateOfBirth | string? | Дата рождения (YYYY-MM-DD) |
| bio | string? | О себе |
| profileVideoUrl | string? | Видео профиля (до 5 сек) — URL |
| isOnline | boolean | Онлайн |

---

### Settings
**Интерфейс.** Настройки приложения (тема, уведомления, приватность).

| Поле | Тип | Описание |
|------|-----|----------|
| theme | 'light' \| 'dark' \| 'system' | Тема |
| notifications | boolean | Уведомления включены |
| sounds | boolean | Звуки |
| vibration | boolean | Вибрация |
| lastSeenPrivacy | 'everyone' \| 'contacts' \| 'nobody' | Кто видит «был(а) недавно» |
| profilePhotoPrivacy | 'everyone' \| 'contacts' \| 'nobody' | Кто видит фото профиля |
| fontSize | 'small' \| 'medium' \| 'large' | Размер шрифта |

---

## 6. Пагинация сообщений в чате

Используется для отображения сообщений в личных чатах, каналах и чатах с ботами. Сейчас реализована статичной переменной; после подключения API подгрузка будет выполняться запросом к серверу.

### MESSAGES_PAGE_SIZE
**Файл:** `src/constants.ts`  
**Тип:** `number` (константа)

Количество сообщений на «страницу»:
- при открытии чата отображаются **N последних** сообщений (N = `MESSAGES_PAGE_SIZE`, по умолчанию 50);
- при **скролле вверх** вызывается подгрузка более ранних сообщений: лимит видимых сообщений увеличивается на `MESSAGES_PAGE_SIZE` (сейчас за счёт уже загруженных данных; при API — дополнительный запрос за более ранними сообщениями).

Значение константы задаётся в коде (сейчас 50). После подключения API тот же размер страницы можно передавать в запросы пагинации (например `limit` / `before`).

### Поведение в UI
- **ChatPage:** контейнер списка сообщений привязан к `onScroll`. Когда пользователь скроллит вверх и `scrollTop` становится меньше порога (80px), вызывается `loadMoreMessages(chatId)`. После обновления списка позиция скролла восстанавливается, чтобы не «прыгало» при появлении старых сообщений сверху.
- **MessagesContext:** хранит по каждому `chatId` «видимый» лимит сообщений (`visibleLimitByChat`). `getMessages(chatId)` возвращает последние `visibleLimit` сообщений; `loadMoreMessages(chatId)` увеличивает этот лимит на `MESSAGES_PAGE_SIZE`; `hasMoreOlder(chatId)` указывает, есть ли ещё более ранние сообщения для подгрузки.

### После подключения API
- При первой загрузке чата: запрос «последние N сообщений» (N = `MESSAGES_PAGE_SIZE`).
- В `loadMoreMessages(chatId)`: запрос за более ранними сообщениями (например по `before_message_id` или `before_timestamp`), добавление результата в кэш/состояние, при необходимости обновление флага «есть ли ещё страницы» для `hasMoreOlder(chatId)`.

---

## 7. Контексты (источники динамических данных)

### AuthContext
**Файл:** `src/context/AuthContext.tsx`

- **phone** — номер авторизованного пользователя (или null).
- **isAuthenticated** — признак входа.
- **pendingPhone** — номер, на который «отправлен» код (шаг ввода кода).
- **requestCode(phone)** — переход на шаг ввода кода.
- **verifyCode(code)** — проверка кода (тестовый код 1234).
- **logout()** — выход.

Данные: начальное состояние из `localStorage` (ключ `messenger-auth`), обновление при входе/выходе.

---

### ChatsContext
**Файл:** `src/context/ChatsContext.tsx`

- **chats** — список чатов (динамический: обновление, пин, мут, архив, удаление).
- **subscribedChannelIds** — Set ID каналов, на которые подписан пользователь.
- **updateChat**, **deleteChat**, **getChatById**, **pinChat**, **muteChat**, **archiveChat**.
- **subscribeToChannel**, **unsubscribeFromChannel**, **isChannelSubscribed**.

Начальные данные: из `mockData` (чаты). В проде — загрузка из API, подписки — из API/локального хранилища.

---

### ContactsContext
**Файл:** `src/context/ContactsContext.tsx`

- **contacts** — список контактов.
- **pinContact**, **archiveContact**, **muteContact**, **blockContact**, **deleteContact**, **addContact**, **getContactById**.

Начальные данные: из `mockData` (contacts). В проде — API.

---

### MessagesContext
**Файл:** `src/context/MessagesContext.tsx`

- **getMessages(chatId)** — последние N сообщений чата (N задаётся пагинацией, см. п. 6). Сейчас из состояния или fallback из mock; в проде — из кэша после загрузки с API.
- **setMessagesForChat**, **addMessageToChat**, **deleteMessage**, **updateMessageReaction**, **incrementViews**.
- **loadMoreMessages(chatId)** — подгрузка более ранних сообщений (при скролле вверх). Сейчас увеличивает видимый лимит; при API — запрос к серверу и добавление в кэш.
- **hasMoreOlder(chatId)** — есть ли ещё более ранние сообщения для подгрузки.

Сообщения хранятся в состоянии по `chatId`; при первом запросе подставляются из `getMessagesForChat` (mock). Отображается не более `visibleLimitByChat[chatId]` последних сообщений (начальное значение и шаг подгрузки — `MESSAGES_PAGE_SIZE`). В проде — API/WebSocket + пагинация при скролле вверх.

---

### CallContext
**Файл:** `src/context/CallContext.tsx`

- **activeCall** — текущая сессия звонка (CallSession \| null).
- **startOutgoingCall**, **setIncomingCall**, **acceptCall**, **declineCall**, **endCall**, **setCallState**, **setNetworkState**.
- **toggleMute**, **toggleSpeaker**, **toggleCamera**, **switchCamera**.
- **callDurationSeconds** — длительность текущего звонка.

История звонков: пока из `mockData` (calls). В проде — API.

---

### StickerContext
**Файл:** `src/context/StickerContext.tsx`

- Панель стикеров: открытие, выбор пака, избранное, недавние, поиск.
- **defaultStickerPacks** — начальный список паков (из `stickerPacks.ts`).
- Избранное и недавние — в `localStorage` (ключи `sticker-favorites`, `sticker-recent`).

В проде: паки и стикеры — из API.

---

### AppStateContext
**Файл:** `src/context/AppStateContext.tsx`

- Глобальное состояние приложения (например боковое меню, модалки). Не хранит доменные сущности из messenger.

---

## 8. Вспомогательные типы (компоненты)

- **AttachAction** (`AttachSheet.tsx`) — действие вложения: фото, видео, файл, контакт и т.д.
- **PinBannerCallbacks** (`PinBanner.tsx`) — колбэки панели закреплённых сообщений.
- **BotKeyboardProps**, **BotButtonsProps** — пропсы компонентов бота.
- **VoiceMessageBubbleProps**, **VideoNoteBubbleProps** — пропсы пузырей голоса/видеокружка.
- **CallContextValue**, **ChatsContextValue**, **ContactsContextValue**, **MessagesContextValue**, **AuthContextValue** — типы контекстов (см. соответствующие провайдеры).

---

## 9. Рекомендации по динамическим данным

1. **Чаты** — загрузка списка с бэкенда, обновление lastMessage и unreadCount через API/WebSocket.
2. **Сообщения** — пагинация по chatId, новые сообщения через WebSocket, реакции и просмотры — API.
3. **Контакты** — список с API, синхронизация блокировки/мута.
4. **Звонки** — история с API; активный звонок — создаётся при инициации/приёме и управляется CallContext.
5. **Текущий пользователь (User)** — после авторизации с API (профиль по токену/phone).
6. **Настройки** — чтение/запись с API или локальное хранилище с синхронизацией.
7. **Стикеры** — список паков и стикеров с API; избранное/недавние — локально или API.
8. **Авторизация** — токен/сессия с бэкенда; телефон и код — через API отправки/проверки кода.

Все статические/моковые источники перечислены в `STATIC_DATA_REPORT.md`.

---

## 10. Личная лента (Feed) — сущности и типы

**Файл типов:** `src/types/feed.ts`  
**Мок-данные:** `src/data/feedMockData.ts`  
**Контекст:** `src/context/FeedContext.tsx`  
**План реализации:** `docs/FEED_IMPLEMENTATION_PLAN.md`

---

### FeedMedia

**Интерфейс.** Один элемент медиа в посте или истории (фото или видео).

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный идентификатор |
| type | 'image' \| 'video' | Тип контента |
| url | string | URL медиа |
| thumbnailUrl | string? | URL превью (опционально) |
| duration | number? | Длительность видео (сек) |

---

### FeedPostVisibility

**Тип:** `'public' | 'followers_only'`

Видимость поста: публичный (все) или только для подписчиков.

---

### FeedPost

**Интерфейс.** Пост в ленте.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| authorId | string | ID автора (FeedUser) |
| media | FeedMedia[] | Медиа: одно или несколько (карусель) |
| caption | string | Подпись, хештеги #tag, теги @username |
| hashtags | string[]? | Хештеги, извлечённые из caption |
| mentionedUserIds | string[]? | Упомянутые userId в подписи |
| createdAt | Date | Время публикации |
| location | string? | Геолокация (название или координаты) |
| visibility | FeedPostVisibility | Публичный / только подписчики |
| likeCount | number | Количество лайков |
| commentCount | number | Количество комментариев |
| likedByCurrentUser | boolean? | Лайкнул ли текущий пользователь |

---

### FeedComment

**Интерфейс.** Комментарий к посту.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| postId | string | ID поста |
| authorId | string | ID автора (FeedUser) |
| parentId | string? | ID родительского комментария (ответ) |
| text | string | Текст комментария |
| createdAt | Date | Время создания |
| likeCount | number | Количество лайков |
| likedByCurrentUser | boolean? | Лайкнул ли текущий пользователь |
| flag | 'important' \| 'spam'? | Пометка модератора |

---

### FeedStory

**Интерфейс.** История (Stories): временная публикация, срок жизни 24 часа.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| authorId | string | ID автора (FeedUser) |
| media | FeedMedia | Один элемент медиа (фото или видео) |
| createdAt | Date | Время публикации |
| expiresAt | Date | Истекает через 24 часа |
| overlayText | string? | Текст/стикеры/фильтры (опционально) |
| viewed | boolean? | Просмотрена ли текущим пользователем (для серой рамки в ленте) |

---

### FeedUser

**Интерфейс.** Пользователь в контексте ленты (профиль для отображения в ленте).

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| name | string | Имя |
| username | string | @username |
| avatar | string? | URL аватара |
| followersCount | number | Количество подписчиков |
| followingCount | number | Количество подписок |
| isFollowedByCurrentUser | boolean? | Подписан ли текущий пользователь |

---

### FeedNotificationType

**Тип:** объединение строк

Тип уведомления ленты:
- `like_post` — лайк поста
- `like_comment` — лайк комментария
- `comment` — комментарий к посту
- `comment_reply` — ответ на комментарий
- `subscribe` — подписка на пользователя
- `new_post` — новый пост (от подписки)
- `friend_request` — заявка в друзья
- `suggested_post` — рекомендуемый пост

---

### FeedNotification

**Интерфейс.** Уведомление ленты.

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Уникальный ID |
| type | FeedNotificationType | Тип уведомления |
| actorId | string | Кто совершил действие (userId) |
| recipientId | string | Кому уведомление (userId) |
| postId | string? | Ссылка на пост |
| commentId | string? | Ссылка на комментарий |
| createdAt | Date | Время создания |
| read | boolean | Прочитано |

---

### FeedContext (лента)

**Файл:** `src/context/FeedContext.tsx`

- **posts**, **comments**, **stories**, **notifications** — состояние ленты.
- **likePost**, **addComment**, **likeComment**, **addPost** — действия с постами и комментариями.
- **getPostById**, **getCommentsByPostId**, **getStoriesByAuthors**, **getFeedUser** — чтение данных.
- **followUser**, **unfollowUser**, **isFollowedByCurrentUser** — подписки.
- **getNotifications**, **markNotificationRead**, **markAllNotificationsRead**, **unreadNotificationsCount** — уведомления.

Начальные данные: из `feedMockData.ts` (initialFeedPosts, initialFeedComments, initialFeedStories, initialFeedNotifications, feedUsers). В проде — API.

---

### Маршруты ленты

| Маршрут | Описание |
|---------|----------|
| `/profile` | В «Дополнительные настройки»: «Создать канал» → `/channel/create`, «Личная лента» → `/feed` |
| `/channel/create` | Форма создания канала |
| `/feed` | Основная лента (посты, истории, нижняя панель) |
| `/feed/search` | Поиск по имени, хештегам, рекомендуемые, фильтры |
| `/feed/create` | Создание поста (медиа, подпись, геолокация, видимость) |
| `/feed/notifications` | Список уведомлений ленты |
| `/feed/profile` | Профиль текущего пользователя в ленте |
| `/feed/profile/:userId` | Профиль другого пользователя (подписаться/отписаться) |
| `/feed/post/:postId` | Экран поста (комментарии, лайк, поделиться) |

---

## 11. Что осталось выполнить для личной ленты

Ниже перечислены пункты из **`docs/FEED_IMPLEMENTATION_PLAN.md`**, которые ещё не реализованы (интеграция с API пропущена по заданию).

### Уже сделано (кратко)

- Блок 0: ссылки «Создать канал» и «Личная лента» на `/profile`.
- 1.1: лента, лайки, комментарии, поделиться, навигация, посты с медиа и счётчиками.
- 1.2: профиль в ленте (свой и чужой), подписчики/подписки, кнопка Подписаться/Отписаться, галерея постов, «Добавить пост».
- 1.3: экран поста, комментарии, лайк, кнопки важное/спам (UI), поделиться.
- 3: создание поста — мультимедиа (карусель), подпись с парсингом хештегов, геолокация, видимость, превью.
- 4: поиск — фильтрация по запросу, рекомендуемые аккаунты, вкладки (популярные, новые, тренды).
- 5: комментарии (добавление, лайк, ответ по parentId).
- 6: уведомления — список, типы, отметка прочитано.
- 7: подписка/отписка, счётчики в профиле.
- 8: истории — отображение по авторам, просмотр по клику (модалка), кнопки «Ответить»/«Поделиться» (заглушки).
- Поделиться в чат: выбор чата из списка, отправка сообщения со ссылкой на пост.
- Создание канала: форма, добавление в ChatsContext.

### Осталось выполнить (без API)

1. ✅ **1.1.5 / 1.3.5** — Увеличение фото/видео поста по нажатию (fullscreen/lightbox) и переключение карусели на экране поста (если несколько медиа). **[ВЫПОЛНЕНО]**
2. ✅ **1.2.5** — В сетке профиля: на каждой картинке показывать лайки и комментарии (счётчики или иконки). **[ВЫПОЛНЕНО]**
3. ✅ **3.2** — Кликабельные хештеги в подписи: переход на поиск/ленту по хештегу. **[ВЫПОЛНЕНО]**
4. **3.6** — Теги пользователей в посте: автодополнение по @имя при создании поста, сохранение mentionedUserIds.
5. ✅ **4.1–4.2** — Автодополнение в поле поиска (по текущим данным). **[ВЫПОЛНЕНО]**
6. **4.7** — Свайп между категориями поиска (горизонтальный) или вертикальный между постами (жесты).
7. ✅ **5.4** — Ответ на комментарий: UI ответа (кнопка «Ответить» под комментарием, отображение цепочки). **[ВЫПОЛНЕНО]**
8. ✅ **5.5** — Удаление комментария: свои — всегда; для автора поста — любые в посте. **[ВЫПОЛНЕНО]**
9. **5.6** — Блокировка пользователя (скрытие комментариев, запрет комментирования — модель и UI).
10. ✅ **6.6** — Фильтры/группировка на экране уведомлений (по типу, по дате). **[ВЫПОЛНЕНО]**
11. **8.3** — Просмотр историй: таймер/автопереход между слайдами, если у автора несколько историй.
12. ✅ **8.5–8.7** — Ответ на историю (отправка сообщения автору), лайк истории, «Поделиться» историей (в чат). **[ВЫПОЛНЕНО: ответ и поделиться]**
13. ✅ **8.8** — Создание истории: экран добавления фото/видео, текст/стикеры/фильтры (опционально). **[ВЫПОЛНЕНО]**
14. ✅ **9.3–9.5** — Настройки конфиденциальности поста (изменить видимость после публикации), настройки приватности профиля, кнопка «Пожаловаться» и связь с модерацией. **[ВЫПОЛНЕНО: изменение видимости поста]**
15. ✅ **11.2** — Жесты: свайп вверх/вниз между постами, свайп влево/вправо между историями или категориями. **[ВЫПОЛНЕНО: свайп между историями]**
16. ✅ **11.5** — Pull-to-refresh для ленты и списков. **[ВЫПОЛНЕНО: кнопка обновления]**
17. **C.2–C.4** — После создания канала: переход в канал, связь с учётной записью; при появлении API — сохранение канала на бэкенде.

### После подключения API

- Все пункты раздела 10 плана (модели данных на бэкенде, REST/GraphQL, пагинация ленты, индикаторы загрузки).
- Пункты 2 (расширенная авторизация), 6.7 (Push), 7.4–7.6 (алгоритмы рекомендаций и лента от подписок).
- Блоки 9 (безопасность и шифрование), 11 (адаптив и жесты), 12 (доп. функции).
