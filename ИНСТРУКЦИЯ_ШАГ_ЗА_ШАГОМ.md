# ПОШАГОВАЯ ИНСТРУКЦИЯ: НАСТРОЙКА ПОДКЛЮЧЕНИЯ

## ШАГ 1: УЗНАТЬ АДРЕС СЕРВЕРА

Вам нужно узнать реальный адрес сервера Beget. Это может быть:
- IP адрес (например: `123.45.67.89`)
- Доменное имя (например: `dragon.beget.com`)

**Где найти:**
- В панели управления Beget
- В письме от Beget при создании сервера
- Спросите у администратора

---

## ШАГ 2: НАСТРОИТЬ SSH НА WINDOWS

### Вариант A: Автоматически (рекомендуется)

1. **Запустите PowerShell в папке проекта:**
   ```powershell
   .\setup-ssh-simple.ps1
   ```

2. **Введите адрес сервера**, когда скрипт спросит

3. **Скопируйте публичный ключ**, который покажет скрипт

### Вариант B: Вручную

1. **Создайте SSH ключ (если еще нет):**
   ```powershell
   ssh-keygen -t rsa -b 4096
   # Нажать Enter для всех вопросов
   ```

2. **Покажите публичный ключ:**
   ```powershell
   type $env:USERPROFILE\.ssh\id_rsa.pub
   ```

3. **Скопируйте ключ** (весь текст, начинающийся с `ssh-rsa`)

4. **Создайте SSH config:**
   - Откройте: `C:\Users\ВашеИмя\.ssh\config`
   - Добавьте:
   ```
   Host dragon
       HostName ВАШ_АДРЕС_СЕРВЕРА
       User dsc23ytp
       IdentityFile C:\Users\ВашеИмя\.ssh\id_rsa
   ```

---

## ШАГ 3: СКОПИРОВАТЬ КЛЮЧ НА СЕРВЕР

### Способ 1: Через SSH (если знаете пароль)

```powershell
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh dsc23ytp@ВАШ_АДРЕС_СЕРВЕРА "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Введите пароль, когда попросит.

### Способ 2: Через панель Beget

1. Войдите в панель управления Beget
2. Найдите раздел "SSH ключи" или "Доступ"
3. Добавьте ваш публичный ключ

### Способ 3: Вручную на сервере

1. Подключитесь к серверу (через панель Beget или SSH с паролем):
   ```bash
   ssh dsc23ytp@ВАШ_АДРЕС_СЕРВЕРА
   ```

2. Выполните команды:
   ```bash
   mkdir -p ~/.ssh
   nano ~/.ssh/authorized_keys
   # Вставьте ваш публичный ключ
   # Сохраните: Ctrl+O, Enter, Ctrl+X
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```

---

## ШАГ 4: ПРОВЕРИТЬ ПОДКЛЮЧЕНИЕ

```powershell
ssh dragon
# или
ssh dsc23ytp@ВАШ_АДРЕС_СЕРВЕРА
```

Если подключилось без пароля - отлично! ✅

---

## ШАГ 5: СОЗДАТЬ СТРУКТУРУ НА СЕРВЕРЕ

Выполните на сервере (через SSH):

```bash
mkdir -p ~/messager/backend
mkdir -p ~/messager/frontend-web
mkdir -p ~/messager/mobile
mkdir -p ~/messager/nginx

# Проверить
ls -la ~/messager/
```

---

## ШАГ 6: СИНХРОНИЗИРОВАТЬ ФАЙЛЫ

На вашем компьютере (Windows):

```powershell
# Обновить адрес сервера в скрипте, если нужно
.\scripts\sync-to-server.ps1
```

Или используйте Git Bash:
```bash
./scripts/sync-to-server.sh
```

---

## ✅ ГОТОВО!

После выполнения всех шагов:
- ✅ SSH настроен
- ✅ Ключи скопированы
- ✅ Структура создана
- ✅ Файлы синхронизированы

Теперь можно работать напрямую с сервером!

---

## 🆘 ЕСЛИ ПРОБЛЕМЫ

### Не знаю адрес сервера

1. Войдите в панель управления Beget
2. Найдите информацию о сервере
3. Или спросите у администратора

### Не подключается SSH

1. Проверьте адрес сервера
2. Проверьте, что ключ скопирован
3. Проверьте права на файлы на сервере

### Не работает синхронизация

1. Используйте Git Bash вместо PowerShell
2. Или используйте WinSCP для ручной синхронизации

---

## 📞 НУЖНА ПОМОЩЬ?

Если что-то не работает, сообщите:
1. Какой адрес сервера вы используете?
2. Какая ошибка появляется?
3. На каком шаге возникла проблема?
