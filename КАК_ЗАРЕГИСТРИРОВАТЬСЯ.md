# 🔐 КАК ЗАРЕГИСТРИРОВАТЬСЯ И ВОЙТИ

## 📍 ТЕКУЩАЯ СИТУАЦИЯ

Вы видите страницу логина на `https://parser-auto.siteaccess.ru/login` - это нормально!

---

## 🚀 КАК ЗАРЕГИСТРИРОВАТЬСЯ

### Вариант 1: Через интерфейс (РЕКОМЕНДУЕТСЯ)

1. **На странице логина** найдите кнопку **"Регистрация"** или **"Зарегистрироваться"**
   - Обычно она находится под формой входа
   - Или перейдите на: `https://parser-auto.siteaccess.ru/register`

2. **Заполните форму регистрации:**
   - **Email:** `test@example.com`
   - **Username:** `testuser`
   - **Password:** `password123` (минимум 6 символов)

3. **Нажмите "Зарегистрироваться"**

4. **После регистрации** вы автоматически войдете в систему

### Вариант 2: Через API (если интерфейс не работает)

Если регистрация через интерфейс не работает, создайте пользователя через API:

```bash
curl -X POST https://parser-auto.siteaccess.ru/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

Затем войдите через интерфейс с этими данными.

### Вариант 3: Через SSH (если API не доступен)

```bash
ssh dsc23ytp@5.101.156.207

# Создать пользователя напрямую через Backend
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

---

## 🔑 КАК ВОЙТИ

После регистрации:

1. **На странице логина** введите:
   - **Email:** `test@example.com` (или тот, который использовали при регистрации)
   - **Password:** `password123` (или тот, который указали)

2. **Нажмите "Войти"**

3. **Вы войдете в систему** и увидите список чатов

---

## ⚠️ ЕСЛИ РЕГИСТРАЦИЯ НЕ РАБОТАЕТ

### Проблема: API не доступен

**Решение:** Нужно настроить проксирование для Apache (файл `.htaccess` уже создан)

**Проверьте:**

1. Файл `.htaccess` должен быть в `public_html/`
2. На сервере должны быть включены модули `mod_proxy` и `mod_rewrite`

**Если не работает:**

Обратитесь в поддержку Beget и попросите:
- Включить модули `mod_proxy` и `mod_rewrite`
- Настроить проксирование `/api` → `http://localhost:3000`

---

## 📋 БЫСТРАЯ ПРОВЕРКА

### Проверить Backend:

```bash
ssh dsc23ytp@5.101.156.207
curl http://localhost:3000/health
```

Должен вернуть: `{"status":"ok"}`

### Проверить API через веб:

```bash
curl https://parser-auto.siteaccess.ru/api/health
```

Если возвращает 404 - нужно настроить проксирование.

---

## ✅ ГОТОВО!

После настройки проксирования вы сможете:
1. Зарегистрироваться через интерфейс
2. Войти в систему
3. Использовать мессенджер
