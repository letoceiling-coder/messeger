# СИНХРОНИЗАЦИЯ ЧЕРЕЗ GIT BASH (РЕКОМЕНДУЕТСЯ)

## 🚀 БЫСТРЫЙ СПОСОБ

Git Bash лучше работает с SSH и rsync. Используйте его для синхронизации.

---

## ИНСТРУКЦИЯ

### 1. Откройте Git Bash в папке проекта

В папке `C:\OSPanel\domains\Messager` правой кнопкой → "Git Bash Here"

### 2. Выполните синхронизацию

```bash
chmod +x scripts/sync-to-server.sh
./scripts/sync-to-server.sh
```

Этот скрипт использует rsync и работает быстрее и надежнее.

---

## АЛЬТЕРНАТИВА: VS CODE REMOTE SSH

Если синхронизация не работает, используйте VS Code Remote SSH:

1. **Установите расширение "Remote - SSH"**
2. **F1 → "Remote-SSH: Connect to Host"**
3. **Введите:** `dsc23ytp@5.101.156.207`
4. **Откройте папку:** `/home/dsc23ytp/messager`
5. **Работайте напрямую с файлами на сервере**

Теперь можно редактировать файлы напрямую на сервере!

---

## РУЧНАЯ СИНХРОНИЗАЦИЯ (если нужно)

Если скрипты не работают, синхронизируйте вручную:

### В Git Bash:

```bash
# Backend
rsync -avz --progress \
  --exclude 'node_modules' \
  --exclude '.git' \
  --exclude 'dist' \
  --exclude 'uploads' \
  --exclude 'logs' \
  --exclude '.env' \
  backend/ dsc23ytp@5.101.156.207:~/messager/backend/

# Frontend
rsync -avz --progress \
  --exclude 'node_modules' \
  --exclude '.git' \
  --exclude 'dist' \
  --exclude '.env' \
  frontend-web/ dsc23ytp@5.101.156.207:~/messager/frontend-web/
```

---

## ПРОВЕРКА

После синхронизации:

```bash
ssh dsc23ytp@5.101.156.207 "ls -la ~/messager/backend/src/"
```

Должны быть файлы проекта.

---

## ✅ РЕКОМЕНДАЦИЯ

**Используйте Git Bash** для синхронизации - он работает лучше с SSH и rsync.

Или **VS Code Remote SSH** для работы напрямую с сервером.
