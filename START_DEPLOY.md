# 🚀 НАЧНИТЕ ЗДЕСЬ: РАЗВЕРТЫВАНИЕ НА СЕРВЕР DRAGON

## ВАШ СЕРВЕР

- **SSH:** `dsc23ytp@dragon`
- **Путь проекта:** `~/messager`
- **Public HTML:** `~/parser-auto.site-access.ru/public_html`

---

## БЫСТРЫЙ СТАРТ (3 ШАГА)

### Шаг 1: Настройка SSH

**Windows:**
```powershell
# См. WINDOWS_SSH_SETUP.md для детальной инструкции
# Или используйте Git Bash (рекомендуется)
```

**Linux/Mac:**
```bash
chmod +x scripts/setup-ssh-config.sh
./scripts/setup-ssh-config.sh
```

**Проверка:**
```bash
ssh dragon
# Должно подключиться
```

### Шаг 2: Создание структуры на сервере

```bash
ssh dragon "mkdir -p ~/messager/{backend,frontend-web,mobile,nginx}"
```

### Шаг 3: Синхронизация и развертывание

**Windows (Git Bash - рекомендуется):**
```bash
chmod +x scripts/*.sh
./scripts/sync-to-server.sh
```

**Windows (PowerShell):**
```powershell
.\scripts\sync-to-server.ps1
```

**Linux/Mac:**
```bash
chmod +x scripts/*.sh
./scripts/sync-to-server.sh
```

---

## ПОЛНОЕ РАЗВЕРТЫВАНИЕ

### Автоматическое (одна команда)

```bash
./scripts/deploy-to-server.sh
```

### Или вручную

См. `QUICK_DEPLOY_DRAGON.md` для детальных инструкций.

---

## РАБОТА НАПРЯМУЮ С СЕРВЕРОМ

### VS Code Remote SSH (рекомендуется)

1. Установите расширение "Remote - SSH"
2. F1 → "Remote-SSH: Connect to Host"
3. Выберите "dragon"
4. Откройте папку: `/home/dsc23ytp/messager`

**Теперь VS Code работает напрямую с файлами на сервере!**

---

## ДОКУМЕНТАЦИЯ

- **`QUICK_DEPLOY_DRAGON.md`** - быстрый старт
- **`WINDOWS_SSH_SETUP.md`** - настройка для Windows
- **`SERVER_CONFIG.md`** - конфигурация сервера
- **`README_SSH.md`** - работа с SSH

---

## ✅ ГОТОВО!

После выполнения этих шагов проект будет на сервере и готов к работе.
