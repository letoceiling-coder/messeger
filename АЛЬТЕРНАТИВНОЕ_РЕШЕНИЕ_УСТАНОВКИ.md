# 🔄 АЛЬТЕРНАТИВНОЕ РЕШЕНИЕ ДЛЯ УСТАНОВКИ ПО

## ⚠️ ПРОБЛЕМА

Не получается войти в веб-терминал - пароль не подходит.

---

## 🎯 РЕШЕНИЕ: УСТАНОВКА ЧЕРЕЗ ПАНЕЛЬ BEGET

### Вариант 1: Быстрая установка ПО (если доступна)

В панели Beget может быть опция **"Быстрая установка ПО"**:

1. Найдите раздел **"Настройки"** или **"Управление"** для VPS
2. Найдите опцию **"Быстрая установка ПО"** или **"Quick software installation"**
3. Выберите нужное ПО:
   - Node.js
   - MySQL
   - Nginx
4. Установите

### Вариант 2: Сбросить пароль

1. В панели Beget найдите раздел **"Настройки"** для VPS
2. Найдите **"Сбросить пароль"** или **"Reset password"**
3. Сбросьте пароль
4. Получите новый пароль
5. Войдите в терминал с новым паролем

### Вариант 3: Использовать SSH с ключом

Если SSH-ключ был добавлен при создании VPS:

```bash
ssh root@89.169.39.244
```

Должно подключиться без пароля (если ключ правильно добавлен).

---

## 📋 ЧТО ДЕЛАТЬ ПОСЛЕ ВХОДА

После того, как войдете в терминал, выполните команды установки:

```bash
# Обновление системы
apt-get update && apt-get upgrade -y

# Установка Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Установка MySQL
apt-get install -y mysql-server
systemctl start mysql
systemctl enable mysql

# Установка Nginx
apt-get install -y nginx
systemctl start nginx
systemctl enable nginx

# Установка PM2
npm install -g pm2

# Установка дополнительных утилит
apt-get install -y git curl wget build-essential

# Создание директорий
mkdir -p /var/www/messager/backend
mkdir -p /var/www/messager/frontend-web
mkdir -p /var/www/messager/backend/uploads
mkdir -p /var/www/messager/backend/logs
```

---

## ✅ РЕКОМЕНДАЦИЯ

**Попробуйте:**
1. Сбросить пароль через панель Beget
2. Или использовать SSH с ключом
3. Или найти опцию "Быстрая установка ПО" в панели

---

## 🚀 ГОТОВО!

**Следующие шаги:**
1. Сбросьте пароль через панель Beget
2. Войдите в терминал с новым паролем
3. Выполните команды установки
4. Сообщите мне - помогу с настройкой проекта!
