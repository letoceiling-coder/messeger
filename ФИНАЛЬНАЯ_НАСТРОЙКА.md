# ФИНАЛЬНАЯ НАСТРОЙКА ПРОЕКТА

## ✅ ЧТО УЖЕ СДЕЛАНО

1. ✅ Backend развернут и работает на порту 3000
2. ✅ Frontend собран и размещен в public_html
3. ✅ База данных MySQL подключена
4. ✅ PM2 запущен
5. ✅ Nginx настроен для проксирования

---

## 🔐 КАК ЗАЙТИ В СИСТЕМУ

### Вариант 1: Регистрация нового пользователя

1. Откройте: `https://parser-auto.siteaccess.ru/`
2. Вы будете перенаправлены на `/login`
3. Нажмите "Регистрация" или перейдите на `/register`
4. Заполните форму:
   - Email: `test@example.com`
   - Username: `testuser`
   - Password: `password123`
5. После регистрации вы автоматически войдете в систему

### Вариант 2: Создать пользователя через API

```bash
curl -X POST https://parser-auto.siteaccess.ru/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

---

## ⚙️ ЧТО ОСТАЛОСЬ СДЕЛАТЬ

### 1. Настроить SSL сертификаты (если еще не настроено)

**Через панель Beget:**
1. Зайдите в панель управления
2. Откройте раздел "SSL"
3. Установите сертификат для домена `parser-auto.siteaccess.ru`

**Или через Let's Encrypt:**
```bash
sudo certbot --nginx -d parser-auto.siteaccess.ru
```

### 2. Проверить работу всех функций

- ✅ Регистрация
- ✅ Логин
- ✅ Создание чата
- ✅ Отправка сообщений
- ✅ WebSocket соединение
- ✅ Голосовые сообщения (если реализовано)

### 3. Настроить Redis (опционально)

Если нужен Redis для масштабирования WebSocket:
1. Включите через панель Beget (2₽/день)
2. Или убедитесь, что REDIS_URL правильный в .env.production

---

## 🧪 ТЕСТИРОВАНИЕ

### Проверка Backend API:

```bash
# Health check
curl https://parser-auto.siteaccess.ru/api/health

# Регистрация
curl -X POST https://parser-auto.siteaccess.ru/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","username":"test","password":"test123"}'

# Логин
curl -X POST https://parser-auto.siteaccess.ru/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test123"}'
```

### Проверка Frontend:

1. Откройте: `https://parser-auto.siteaccess.ru/`
2. Должен быть редирект на `/login`
3. Зарегистрируйтесь или войдите
4. Проверьте работу чата

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Если не работает API:

```bash
# Проверить Backend
ssh dsc23ytp@5.101.156.207
pm2 status
pm2 logs messager-backend

# Проверить Nginx
sudo nginx -t
sudo systemctl status nginx
```

### Если не работает Frontend:

```bash
# Проверить файлы
ls -la ~/parser-auto.site-access.ru/public_html/

# Пересобрать Frontend
cd ~/messager/frontend-web
npm run build
cp -r dist/* ~/parser-auto.site-access.ru/public_html/
```

---

## ✅ ГОТОВО!

Проект полностью развернут и готов к использованию!

**Доступ:**
- Frontend: `https://parser-auto.siteaccess.ru/`
- Backend API: `https://parser-auto.siteaccess.ru/api`
- WebSocket: `wss://parser-auto.siteaccess.ru/socket.io/`
