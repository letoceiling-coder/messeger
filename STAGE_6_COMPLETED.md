# Этап 6 — Меню настроек (расширенное) ✅

Дата завершения: 2026-01-30

---

## ✅ Этап 6: Меню настроек — Полная реализация

### Что было ранее:
- ✅ Профиль с редактированием имени и аватара
- ✅ Кнопка настроек (иконка шестерёнки) в ChatsPage
- ✅ Backend API для обновления профиля
- ✅ Навигация в `/settings`

### Что добавлено сейчас:

---

## 6.1 Настройки уведомлений ✅

### Реализованные настройки:

#### 1. **Уведомления (глобальный переключатель)**
- Включить/выключить все уведомления
- При выключении — блокируются звук и вибрация
- Хранится в `localStorage`: `settings_notifications_enabled`
- По умолчанию: **включено**

#### 2. **Звук**
- Воспроизведение звука при новых сообщениях
- Зависит от глобального переключателя уведомлений
- Хранится в `localStorage`: `settings_sound_enabled`
- По умолчанию: **включено**

#### 3. **Вибрация**
- Вибрация при новых сообщениях (для мобильных устройств)
- Зависит от глобального переключателя уведомлений
- Хранится в `localStorage`: `settings_vibration_enabled`
- По умолчанию: **включено**

### UI компоненты:
- **Toggle Switch** — кастомный переключатель в стиле iOS
- Цвета: `app-accent` (включено), `app-surface-hover` (выключено)
- Плавные анимации: `transition-colors`, `transition-transform`
- Описания под каждой настройкой
- Disabled-состояние для зависимых настроек

---

## 6.2 Настройки конфиденциальности ✅

### Реализованные настройки:

#### 1. **Статус "в сети"**
- Видимость онлайн-статуса для других пользователей
- Хранится в `localStorage`: `settings_show_online_status`
- По умолчанию: **включено**
- Toast-уведомление при изменении

#### 2. **Последняя активность ("был(а) в сети")**
- Видимость времени последней активности
- Хранится в `localStorage`: `settings_show_last_seen`
- По умолчанию: **включено**
- Toast-уведомление при изменении

### UI компоненты:
- Те же **Toggle Switch** компоненты
- Описания под каждой настройкой
- Мгновенное сохранение при переключении

---

## Технические детали

### Хранение настроек
```typescript
// localStorage ключи:
- settings_notifications_enabled: 'true' | 'false'
- settings_sound_enabled: 'true' | 'false'
- settings_vibration_enabled: 'true' | 'false'
- settings_show_online_status: 'true' | 'false'
- settings_show_last_seen: 'true' | 'false'
```

### Инициализация
```typescript
const [soundEnabled, setSoundEnabled] = useState(() => {
  const saved = localStorage.getItem('settings_sound_enabled');
  return saved !== null ? saved === 'true' : true; // по умолчанию true
});
```

### Сохранение
```typescript
const handleToggleSound = (enabled: boolean) => {
  setSoundEnabled(enabled);
  localStorage.setItem('settings_sound_enabled', String(enabled));
  showSuccess(enabled ? 'Звук включён' : 'Звук выключен');
};
```

---

## Структура страницы настроек

### Разделы (по порядку):

1. **Профиль**
   - Аватар (клик для загрузки)
   - Имя пользователя (редактирование)
   - Email (только чтение)
   - Кнопка "Сохранить изменения"

2. **Уведомления** (новое)
   - Уведомления (главный переключатель)
   - Звук (зависит от главного)
   - Вибрация (зависит от главного)

3. **Конфиденциальность** (новое)
   - Статус "в сети"
   - Последняя активность

4. **Выход**
   - Кнопка "Выйти из аккаунта"

---

## UI/UX особенности

### Toggle Switch компонент:
```tsx
<button
  onClick={() => handleToggle(!enabled)}
  className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${
    enabled ? 'bg-app-accent' : 'bg-app-surface-hover'
  }`}
>
  <span
    className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${
      enabled ? 'translate-x-6' : 'translate-x-1'
    }`}
  />
</button>
```

### Визуальный стиль:
- **Размер переключателя:** 44×24px (h-6 w-11)
- **Размер кружка:** 16×16px (h-4 w-4)
- **Отступы:** translate-x-1 (выкл), translate-x-6 (вкл)
- **Цвета:** `bg-app-accent` / `bg-app-surface-hover`
- **Анимации:** плавные переходы

### Карточки настроек:
- `bg-app-surface` с `rounded-xl`
- Padding: `py-3 px-4`
- Описания: `text-xs text-app-text-secondary`
- Заголовки: `text-sm font-medium text-app-text`

---

## Toast-уведомления

Каждое изменение настройки показывает Toast:

```typescript
// Примеры сообщений:
showSuccess('Звук уведомлений включён');
showSuccess('Звук уведомлений выключен');
showSuccess('Статус "в сети" виден другим');
showSuccess('Статус "в сети" скрыт');
// и т.д.
```

---

## Интеграция

### Использование настроек в других компонентах:

```typescript
// Пример: проверка перед воспроизведением звука
const soundEnabled = localStorage.getItem('settings_sound_enabled') === 'true';
const notificationsEnabled = localStorage.getItem('settings_notifications_enabled') === 'true';

if (soundEnabled && notificationsEnabled) {
  soundService.playNotification();
}

// Пример: проверка перед показом онлайн-статуса
const showOnlineStatus = localStorage.getItem('settings_show_online_status') === 'true';
if (showOnlineStatus) {
  // Показать зелёную точку "онлайн"
}
```

---

## Будущие улучшения (опционально)

### Backend-синхронизация:
- Хранить настройки на сервере (таблица `user_settings`)
- API: `GET /users/me/settings`, `PATCH /users/me/settings`
- Синхронизация между устройствами

### Дополнительные настройки:
- Выбор мелодии уведомлений
- Уровень громкости
- Время работы "Не беспокоить"
- Настройки для групповых чатов отдельно

---

## Файлы

### Обновлённые:
- `frontend-web/src/pages/SettingsPage.tsx` (+150 строк)

### Новые:
- `STAGE_6_COMPLETED.md` (этот файл)

---

## Итоги этапов 0-11+6

### ✅ Выполнено:
- **Этап 0** — Быстрые победы (UX)
- **Этап 1** — Поле ввода Telegram
- **Этап 2** — Запись аудио
- **Этап 4** — Эмодзи (192 шт, 8 категорий)
- **Этап 3+8** — Фото/Видео
- **Этап 11** — Скелетоны загрузки
- **Этап 6** — Настройки (профиль, уведомления, конфиденциальность) ✅

### 📊 Статистика:
- **Готовность базового UI:** ~65% ✅
- **Новых/обновлённых файлов:** 15+
- **Строк кода:** ~2000+

---

## Следующие этапы (по плану)

### 🔜 Этап 5: Список контактов и поиск
- Фильтры по недавним/активности
- **Поиск по чатам** (поле поиска над списком)
- Layout для desktop

### 🔜 Этап 7: Статусы сообщений
- Визуализация: отправлено ✓ / доставлено ✓✓ / прочитано ✓✓ (синие)
- WebSocket-события для обновления статусов

### 🔜 Этап 9: Звонки (доработки)
- Улучшить UI входящего звонка
- Кнопка "Отклонить" заметнее (красная, крупнее)
- Вибрация на мобильном

### 🔜 Этап 10: Долгое нажатие и контекстное меню
- Ответить, переслать, удалить (уже есть частично)
- Редактировать сообщения

---

**Статус проекта:** Основной UI и настройки завершены ✅  
**Готовность:** ~65% базового функционала  
**Следующий фокус:** Поиск по чатам → Статусы → Звонки → Долгое нажатие
