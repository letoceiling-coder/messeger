# ✅ SSH КЛЮЧ ДОБАВЛЕН! СЛЕДУЮЩИЕ ШАГИ

## 🎉 ОТЛИЧНО!

Вы успешно добавили SSH ключ на сервер. Теперь нужно настроить подключение с вашего компьютера.

---

## ШАГ 1: ПРОВЕРИТЬ ПОДКЛЮЧЕНИЕ

### На вашем компьютере (Windows)

**Попробуйте подключиться:**

```bash
ssh dsc23ytp@parser-auto.site-access.ru
```

**Или если знаете IP адрес:**
```bash
ssh dsc23ytp@IP_АДРЕС_СЕРВЕРА
```

**Если подключилось БЕЗ ПАРОЛЯ** - отлично! ✅

**Если все еще требует пароль:**
- Проверьте, что ключ правильно добавлен на сервере
- Проверьте права: `ssh parser-auto.site-access.ru "ls -la ~/.ssh/"`

---

## ШАГ 2: НАСТРОИТЬ SSH CONFIG (ОПЦИОНАЛЬНО)

Это позволит подключаться просто: `ssh dragon`

### Автоматически

```powershell
.\setup-ssh-simple.ps1
```

Введите адрес сервера: `parser-auto.site-access.ru` (или IP адрес)

### Вручную

1. Откройте файл: `C:\Users\ВашеИмя\.ssh\config`
2. Добавьте:
   ```
   Host dragon
       HostName parser-auto.site-access.ru
       User dsc23ytp
       IdentityFile C:\Users\ВашеИмя\.ssh\id_rsa
   ```

После этого можно подключаться: `ssh dragon`

---

## ШАГ 3: ОБНОВИТЬ СКРИПТЫ

Скрипты уже обновлены для использования адреса `parser-auto.site-access.ru`.

Если нужно использовать другой адрес, откройте:
- `scripts/sync-to-server.ps1` - для PowerShell
- `scripts/sync-to-server.sh` - для Git Bash

И замените адрес сервера.

---

## ШАГ 4: СИНХРОНИЗИРОВАТЬ ФАЙЛЫ

После проверки подключения:

**PowerShell:**
```powershell
.\scripts\sync-to-server.ps1
```

**Git Bash:**
```bash
./scripts/sync-to-server.sh
```

Это скопирует все файлы проекта на сервер в `~/messager/`.

---

## ШАГ 5: НАСТРОИТЬ ПРОЕКТ НА СЕРВЕРЕ

После синхронизации подключитесь к серверу:

```bash
ssh dsc23ytp@parser-auto.site-access.ru
```

И выполните настройку (см. `СЛЕДУЮЩИЕ_ШАГИ.txt` или `QUICK_DEPLOY_DRAGON.md`)

---

## ✅ ПРОВЕРКА

### Проверить структуру на сервере

```bash
ssh dsc23ytp@parser-auto.site-access.ru "ls -la ~/messager/"
```

Должны быть директории:
- `backend/`
- `frontend-web/`
- `mobile/`
- `nginx/`

### Проверить SSH ключ

```bash
ssh dsc23ytp@parser-auto.site-access.ru "cat ~/.ssh/authorized_keys"
```

Должен быть ваш ключ.

---

## 🆘 ЕСЛИ ПРОБЛЕМЫ

### Не подключается без пароля

1. Проверьте ключ на сервере:
   ```bash
   ssh dsc23ytp@parser-auto.site-access.ru "cat ~/.ssh/authorized_keys"
   ```

2. Сравните с вашим ключом:
   ```powershell
   type $env:USERPROFILE\.ssh\id_rsa.pub
   ```

3. Если не совпадает, добавьте ключ снова (см. `СКОПИРУЙТЕ_ЭТИ_КОМАНДЫ.txt`)

### Не знаю адрес сервера

- Используйте домен: `parser-auto.site-access.ru`
- Или найдите IP адрес в панели управления Beget

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ ФАЙЛЫ

- **`СЛЕДУЮЩИЕ_ШАГИ.txt`** - что делать дальше
- **`ПРОВЕРКА_ПОДКЛЮЧЕНИЯ.md`** - проверка подключения
- **`QUICK_DEPLOY_DRAGON.md`** - полное развертывание

---

## 💡 СОВЕТ

После успешной настройки:
- Используйте VS Code Remote SSH для удобной работы
- Используйте скрипты для автоматической синхронизации
- Работайте напрямую с файлами на сервере
