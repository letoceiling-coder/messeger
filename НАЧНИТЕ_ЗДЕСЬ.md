# 🚀 НАЧНИТЕ ЗДЕСЬ: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ DRAGON

## ВАШ СЕРВЕР

- **SSH:** `dsc23ytp@dragon`
- **Путь проекта:** `~/messager`
- **Public HTML:** `~/parser-auto.site-access.ru/public_html`

---

## БЫСТРЫЙ СТАРТ (Windows)

### Вариант 1: Git Bash (рекомендуется)

1. **Установите Git for Windows** (если еще не установлен)
   - Скачайте с: https://git-scm.com/download/win

2. **Откройте Git Bash** в папке проекта

3. **Настройте SSH:**
   ```bash
   # Генерация ключа (если еще нет)
   ssh-keygen -t rsa -b 4096
   # Нажать Enter для всех вопросов
   
   # Копирование ключа на сервер
   cat ~/.ssh/id_rsa.pub | ssh dsc23ytp@dragon "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
   ```

4. **Создайте структуру на сервере:**
   ```bash
   ssh dsc23ytp@dragon "mkdir -p ~/messager/{backend,frontend-web,mobile,nginx}"
   ```

5. **Синхронизируйте файлы:**
   ```bash
   chmod +x scripts/*.sh
   ./scripts/sync-to-server.sh
   ```

### Вариант 2: PowerShell

1. **Настройте SSH** (см. `WINDOWS_SSH_SETUP.md`)

2. **Синхронизируйте файлы:**
   ```powershell
   .\scripts\sync-to-server.ps1
   ```

---

## VS CODE REMOTE SSH (РЕКОМЕНДУЕТСЯ)

### Настройка

1. **Установите расширение:**
   - Откройте VS Code
   - Extensions → найдите "Remote - SSH"
   - Установите

2. **Подключитесь к серверу:**
   - Нажмите F1
   - Выберите "Remote-SSH: Connect to Host"
   - Введите: `dsc23ytp@dragon`
   - Или выберите "dragon" если настроен SSH config

3. **Откройте папку проекта:**
   - После подключения: File → Open Folder
   - Введите: `/home/dsc23ytp/messager`

**Теперь VS Code работает напрямую с файлами на сервере!**

---

## ПЕРВОЕ РАЗВЕРТЫВАНИЕ

### На сервере (через SSH или VS Code Remote)

```bash
# Подключиться
ssh dsc23ytp@dragon
cd ~/messager

# Backend
cd backend
cp .env.production.example .env.production
nano .env.production  # Заполнить значения
npm install --production
npx prisma generate
npx prisma migrate deploy
npm run build
mkdir -p uploads/audio logs

# Frontend
cd ../frontend-web
cp .env.production.example .env.production
nano .env.production  # Заполнить значения
npm install
npm run build
cp -r dist/* ~/parser-auto.site-access.ru/public_html/

# Запуск Backend (PM2)
cd ../backend
npm install -g pm2
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

---

## ОБНОВЛЕНИЕ ПРОЕКТА

### Быстрое обновление

```bash
# Синхронизация изменений
./scripts/sync-to-server.sh

# На сервере
ssh dsc23ytp@dragon
cd ~/messager/backend
npm run build
pm2 restart messager-backend

cd ../frontend-web
npm run build
cp -r dist/* ~/parser-auto.site-access.ru/public_html/
```

### Автоматическое развертывание

```bash
./scripts/deploy-to-server.sh
```

---

## ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Подключение к серверу
ssh dsc23ytp@dragon

# Работа с проектом
ssh dsc23ytp@dragon "cd ~/messager/backend && ls -la"

# Просмотр логов
ssh dsc23ytp@dragon "pm2 logs messager-backend"

# Перезапуск Backend
ssh dsc23ytp@dragon "cd ~/messager/backend && pm2 restart messager-backend"
```

---

## ДОКУМЕНТАЦИЯ

- **`START_DEPLOY.md`** - быстрый старт
- **`QUICK_DEPLOY_DRAGON.md`** - детальное руководство
- **`WINDOWS_SSH_SETUP.md`** - настройка SSH для Windows
- **`SERVER_CONFIG.md`** - конфигурация сервера
- **`README_SSH.md`** - работа с SSH

---

## ✅ ГОТОВО!

После выполнения этих шагов:
- ✅ Проект синхронизирован с сервером
- ✅ Можно работать напрямую через VS Code Remote SSH
- ✅ Можно обновлять проект через скрипты

**Приятной работы!** 🚀
