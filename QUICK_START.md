# БЫСТРЫЙ СТАРТ

## 🚀 ЗАПУСК ПРОЕКТА

### 1. Backend

```bash
cd backend

# Установить зависимости (если еще не установлены)
npm install

# Настроить .env
cp .env.example .env
# Заполнить DATABASE_URL, JWT_SECRET, REDIS_URL, TELEGRAM_BOT_TOKEN

# Запустить миграции
npx prisma migrate dev
npx prisma generate

# Запустить сервер
npm run start:dev
```

### 2. Redis (для масштабируемости)

```bash
# Через Docker
docker run -d -p 6379:6379 redis:7-alpine

# Или установить локально
# Windows: choco install redis
# macOS: brew install redis
# Linux: apt-get install redis
```

### 3. Frontend Web

```bash
cd frontend-web

# Установить зависимости
npm install

# Настроить .env
# VITE_API_URL=http://localhost:3000
# VITE_WS_URL=http://localhost:3000

# Запустить
npm run dev
```

### 4. Mobile (Expo)

```bash
cd mobile

# Установить зависимости
npm install

# Запустить
npx expo start
```

---

## 🔐 E2EE НАСТРОЙКА

### Автоматическая инициализация

E2EE автоматически инициализируется при открытии чата:
1. Генерируются RSA ключи (если еще не созданы)
2. Публичный ключ сохраняется на сервере
3. Обменивается AES ключ с другим участником
4. Сообщения автоматически шифруются

### Ручная настройка (опционально)

Если нужно принудительно включить/выключить шифрование, можно добавить переключатель в UI.

---

## 📱 TELEGRAM MINIAPP

### Создание Bot

1. Открыть @BotFather в Telegram
2. Отправить `/newbot`
3. Следовать инструкциям
4. Получить Bot Token
5. Отправить `/newapp` и выбрать бота
6. Указать URL MiniApp: `https://yourdomain.com/telegram`
7. Добавить Bot Token в `backend/.env`

### Frontend для Telegram

Создать страницу `frontend-web/src/pages/TelegramApp.tsx`:
- Использовать Telegram WebApp API
- Автоматическая аутентификация через initData
- Адаптация UI под Telegram стиль

---

## ✅ ПРОВЕРКА РАБОТЫ

### E2EE
1. Зарегистрировать двух пользователей
2. Создать чат между ними
3. Отправить сообщение
4. Проверить индикатор 🔒 E2EE в заголовке чата
5. Проверить в БД, что сообщение зашифровано

### Redis
1. Запустить два экземпляра Backend:
   ```bash
   PORT=3000 npm run start:dev
   PORT=3001 npm run start:dev
   ```
2. Подключить клиентов к разным серверам
3. Отправить сообщение
4. Проверить доставку на другом сервере

### Telegram
1. Создать Bot через @BotFather
2. Настроить MiniApp URL
3. Открыть MiniApp в Telegram
4. Проверить автоматическую аутентификацию

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка подключения к Redis
- Проверить, что Redis запущен: `redis-cli ping`
- Проверить REDIS_URL в .env
- Backend продолжит работу без Redis (с предупреждением)

### Ошибка E2EE
- Проверить, что публичные ключи сохранены
- Проверить консоль браузера на ошибки
- Убедиться, что Web Crypto API поддерживается

### Ошибка Telegram Auth
- Проверить Bot Token в .env
- Проверить, что initData валиден
- Проверить время (не старше 24 часов)

---

## 📚 ДОКУМЕНТАЦИЯ

- `E2EE_ARCHITECTURE.md` - архитектура E2EE
- `REDIS_SCALING_ARCHITECTURE.md` - архитектура Redis
- `TELEGRAM_INTEGRATION_PLAN.md` - план интеграции Telegram
- `IMPLEMENTATION_GUIDE.md` - руководство по реализации
- `FINAL_IMPLEMENTATION_STATUS.md` - финальный статус
