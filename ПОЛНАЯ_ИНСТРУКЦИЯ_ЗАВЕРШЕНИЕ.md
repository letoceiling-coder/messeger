# 📋 ПОЛНАЯ ИНСТРУКЦИЯ ДЛЯ ЗАВЕРШЕНИЯ РЕАЛИЗАЦИИ

## ⚠️ ТЕКУЩАЯ СИТУАЦИЯ

### ✅ Что работает:
- Frontend открывается (страница логина)
- Backend запущен на `localhost:30000`
- База данных подключена

### ❌ Что не работает:
- **API недоступен через веб** - Apache не проксирует `/api` к Backend
- **Авторизация не работает** - запросы к `/api/auth/login` возвращают 404
- **Чат не работает** - WebSocket недоступен (тоже нужен проксинг)

---

## 🔧 РЕШЕНИЕ: НАСТРОИТЬ ПРОКСИРОВАНИЕ APACHE

### Проблема:
`ProxyPass` **не работает в файле `.htaccess`** - он работает только в **основной конфигурации Apache**.

На shared hosting Beget у вас нет доступа к основной конфигурации, поэтому нужно обратиться в поддержку.

---

## 📞 ШАГ 1: ОБРАТИТЬСЯ В ПОДДЕРЖКУ BEGET

### Инструкция:

1. **Зайдите в панель:** `https://cp.beget.com`
2. **Откройте раздел:** "Поддержка" (Support)
3. **Создайте новый тикет**
4. **Скопируйте и отправьте следующий текст:**

```
Здравствуйте!

Нужна помощь с настройкой Apache для проксирования Node.js приложения.

Сайт: parser-auto.siteaccess.ru
Backend работает на: http://localhost:30000

ПРОБЛЕМА:
- API запросы к /api/* возвращают 404 Not Found
- WebSocket соединения не работают
- Приложение не может подключиться к Backend

ЧТО НУЖНО:

1. Включить модули Apache:
   - mod_proxy
   - mod_proxy_http
   - mod_rewrite

2. Настроить проксирование в основной конфигурации Apache:

   # Backend API
   <Location /api>
       ProxyPass http://localhost:30000/api
       ProxyPassReverse http://localhost:30000/api
       ProxyPreserveHost On
   </Location>

   # WebSocket для Socket.io
   <Location /socket.io>
       ProxyPass http://localhost:30000/socket.io
       ProxyPassReverse http://localhost:30000/socket.io
       ProxyPreserveHost On
       
       # WebSocket заголовки
       RewriteEngine on
       RewriteCond %{HTTP:Upgrade} websocket [NC]
       RewriteCond %{HTTP:Connection} upgrade [NC]
       RewriteRule ^/?(.*) "ws://localhost:30000/$1" [P,L]
   </Location>

3. Или использовать альтернативный вариант через RewriteRule:

   RewriteEngine On
   RewriteCond %{REQUEST_URI} ^/api/(.*)$
   RewriteRule ^/api/(.*)$ http://localhost:30000/api/$1 [P,L]
   
   RewriteCond %{REQUEST_URI} ^/socket.io/(.*)$
   RewriteRule ^/socket.io/(.*)$ http://localhost:30000/socket.io/$1 [P,L]

ВАЖНО:
- Backend уже запущен и работает на localhost:30000
- Файл .htaccess не может использовать ProxyPass (ограничение Apache)
- Нужна настройка в основной конфигурации виртуального хоста

Помогите, пожалуйста, настроить проксирование.

Спасибо!
```

---

## 🔍 ШАГ 2: ПРОВЕРИТЬ BACKEND

Перед обращением в поддержку убедитесь, что Backend работает:

```bash
ssh dsc23ytp@5.101.156.207

# Проверить, что Backend запущен
curl http://localhost:30000/health

# Должен вернуть: {"status":"ok",...}
```

Если Backend не запущен, запустите его:

```bash
cd ~/messager/backend

export JWT_SECRET='iIPvW1ifTAzaH7FOu2Q19o4sFSHlcLMekdOIFzmkanE='
export DATABASE_URL='mysql://dsc23ytp_mess:r7nCbBSN%cr3@localhost:3306/dsc23ytp_mess'
export NODE_ENV=production
export PORT=30000
export CORS_ORIGIN='https://parser-auto.siteaccess.ru'

nohup node dist/main.js > backend.log 2>&1 &
```

---

## 📋 ШАГ 3: СОЗДАТЬ ПОЛЬЗОВАТЕЛЯ

После настройки проксирования создайте пользователя:

```bash
ssh dsc23ytp@5.101.156.207

curl -X POST http://localhost:30000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

Или через веб-интерфейс после настройки проксирования.

---

## ✅ ШАГ 4: ПРОВЕРКА ПОСЛЕ НАСТРОЙКИ

После того, как поддержка настроит проксирование:

### 1. Проверьте API:

```bash
curl https://parser-auto.siteaccess.ru/api/health
```

Должен вернуть: `{"status":"ok",...}`

### 2. Проверьте авторизацию:

Откройте: `https://parser-auto.siteaccess.ru/login`

Введите:
- Email: `admin@example.com`
- Password: `admin123`

Должен пройти вход.

### 3. Проверьте WebSocket:

Откройте консоль браузера (F12) и проверьте, что WebSocket подключается.

---

## 🔌 О WEBSOCKET

### Почему не работает:
WebSocket требует специальной настройки проксирования с заголовками `Upgrade` и `Connection`.

### Что нужно:
1. **Проксирование `/socket.io`** к `localhost:30000/socket.io`
2. **Заголовки для WebSocket:**
   - `Upgrade: websocket`
   - `Connection: upgrade`

### После настройки:
- WebSocket автоматически заработает
- Чат будет работать в реальном времени
- Видеозвонки будут работать

---

## 📊 ПЛАН ДЕЙСТВИЙ

### Сейчас:
1. ✅ Проверить, что Backend запущен
2. ✅ Обратиться в поддержку Beget
3. ⏳ Дождаться настройки проксирования

### После настройки:
1. ✅ Проверить API (`/api/health`)
2. ✅ Создать пользователя
3. ✅ Проверить авторизацию
4. ✅ Проверить WebSocket
5. ✅ Протестировать чат

---

## 🎯 ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Backend запущен на `localhost:30000`
- [ ] Обращение в поддержку Beget отправлено
- [ ] Проксирование настроено поддержкой
- [ ] API доступен через веб (`/api/health`)
- [ ] Пользователь создан
- [ ] Авторизация работает
- [ ] WebSocket подключен
- [ ] Чат работает

---

## ✅ ГОТОВО!

**Следующие шаги:**
1. Проверьте Backend (см. Шаг 2)
2. Обратитесь в поддержку Beget (см. Шаг 1)
3. Дождитесь настройки проксирования
4. Протестируйте все функции

**После настройки проксирования все будет работать!**
