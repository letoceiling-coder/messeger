# 📋 ИНСТРУКЦИЯ ПО КОПИРОВАНИЮ ФАЙЛОВ НА VPS

## Проблема

Файлы не копируются через `scp` из PowerShell. Нужно использовать другой метод.

## Решение

### Вариант 1: Использовать Git (если проект в репозитории)

```bash
ssh root@89.169.39.244
cd /var/www/messager
git clone <ваш-репозиторий> backend
cd backend
npm install
npm run build
```

### Вариант 2: Использовать WinSCP или FileZilla

1. Подключитесь к `89.169.39.244` через WinSCP/FileZilla
2. Скопируйте папку `backend` в `/var/www/messager/`
3. Скопируйте папку `frontend-web/dist` в `/var/www/messager/frontend-web/`

### Вариант 3: Использовать VS Code Remote SSH

1. Установите расширение "Remote - SSH" в VS Code
2. Подключитесь к `root@89.169.39.244`
3. Откройте папку `/var/www/messager`
4. Скопируйте файлы через интерфейс VS Code

### Вариант 4: Создать архив и передать

На Windows:
```powershell
cd backend
Compress-Archive -Path * -DestinationPath ../backend.zip -Force
scp ../backend.zip root@89.169.39.244:/tmp/
```

На сервере:
```bash
ssh root@89.169.39.244
cd /var/www/messager
unzip /tmp/backend.zip -d backend
cd backend
npm install
npm run build
```

---

## Текущий статус

Скопированы базовые файлы:
- ✅ `package.json`
- ✅ `tsconfig.json`
- ✅ `nest-cli.json`
- ✅ `ecosystem.config.js`
- ✅ `prisma/schema.prisma`
- ✅ `.env.production`

**Нужно скопировать:**
- ⚠️ `src/` - весь исходный код
- ⚠️ `prisma/migrations/` - миграции базы данных

**Рекомендация:** Используйте WinSCP или VS Code Remote SSH для удобного копирования.
