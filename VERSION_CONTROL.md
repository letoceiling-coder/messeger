# 🔄 СИСТЕМА ВЕРСИОНИРОВАНИЯ И ОБХОДА КЕША

## ✅ ЧТО СДЕЛАНО (31.01.2026)

### 1. Meta-теги в index.html
Добавлены жёсткие запреты кеширования:
```html
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
<meta http-equiv="Pragma" content="no-cache" />
<meta http-equiv="Expires" content="0" />
```

### 2. Улучшенная конфигурация Nginx
Усилены заголовки для index.html:
```nginx
location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate, proxy-revalidate, max-age=0";
    add_header Pragma "no-cache";
    add_header Expires "0";
    etag off;
    if_modified_since off;
}
```

### 3. Автоматические хеши файлов
Vite создаёт уникальные имена файлов при каждой сборке:
```
index-DgNrL6LH.js  ← хеш меняется при изменении кода
index-rGA2pW_o.css ← хеш меняется при изменении стилей
```

### 4. .env.example для документации
Создан шаблон с описанием всех переменных окружения.

---

## 🎯 КАК ЭТО РАБОТАЕТ

### Многоуровневая защита от кеша:

```
1. Браузер запрашивает: https://neekloai.ru
   └─> Nginx отдаёт index.html с заголовками "no-cache"
   
2. Браузер КАЖДЫЙ РАЗ загружает свежий index.html
   └─> В index.html указаны JS/CSS с НОВЫМИ хешами
   
3. Браузер видит новый хеш (index-XYZ123.js)
   └─> Загружает новый файл (старый хеш больше не используется)

4. JS/CSS кешируются на 1 год (immutable)
   └─> Но при новой сборке хеш меняется → загружается новая версия
```

### Пример:

**Старая сборка:**
```html
<script src="/assets/index-DgNrL6LH.js"></script>
```

**После обновления и пересборки:**
```html
<script src="/assets/index-BnF8kP2M.js"></script>  ← НОВЫЙ ХЕШ!
```

Браузер увидит новое имя файла и загрузит его, игнорируя старый кеш!

---

## 🚀 ПРОЦЕСС ДЕПЛОЯ БЕЗ ПРОБЛЕМ С КЕШЕМ

### Шаг 1: Обновление кода
```bash
cd /var/www/messager
git pull origin main
```

### Шаг 2: Пересборка фронтенда
```bash
cd frontend-web
npm run build
```

**Vite автоматически:**
- ✅ Создаст новые файлы с НОВЫМИ хешами
- ✅ Обновит index.html со ссылками на новые файлы
- ✅ Удалит старые файлы из dist/

### Шаг 3: Перезагрузка Nginx
```bash
nginx -t && nginx -s reload
```

### Шаг 4: Проверка
```bash
# Посмотреть какие файлы созданы
ls -lh /var/www/messager/frontend-web/dist/assets/index-*.js

# Должен быть НОВЫЙ хеш, отличный от старого
```

---

## 📊 ГАРАНТИИ

### ✅ 100% обход кеша при обновлении:

| Уровень | Механизм | Гарантия |
|---------|----------|----------|
| 1 | Meta-теги в HTML | ⭐⭐⭐⭐ |
| 2 | Nginx заголовки no-cache | ⭐⭐⭐⭐⭐ |
| 3 | Отключение ETag | ⭐⭐⭐⭐ |
| 4 | Отключение if_modified_since | ⭐⭐⭐⭐ |
| 5 | Уникальные хеши файлов | ⭐⭐⭐⭐⭐ |
| **ИТОГО** | **Многоуровневая защита** | **⭐⭐⭐⭐⭐** |

---

## 🧪 ТЕСТИРОВАНИЕ

### После деплоя проверьте:

1. **Откройте DevTools (F12) → Network**
2. **Обновите страницу (Ctrl+Shift+R)**
3. **Проверьте:**
   - index.html загружается заново (не из кеша)
   - index-[ХЕШ].js имеет НОВЫЙ хеш
   - Response Headers содержат `Cache-Control: no-cache`

---

## 🔧 ПЕРЕКЛЮЧЕНИЕ TURN БЕЗ ПРОБЛЕМ С КЕШЕМ

### Теперь можно легко переключаться:

#### На собственный TURN:
```bash
ssh root@89.169.39.244
cat > /var/www/messager/frontend-web/.env.production << 'EOF'
VITE_TURN_USER=turn_fdf8b6e8
VITE_TURN_CREDENTIAL=U1cM4fhoxxqTnbb8XE9n
VITE_TURN_SERVER=89.169.39.244
EOF

cd /var/www/messager/frontend-web
npm run build
```

#### На облачный Metered.ca:
```bash
ssh root@89.169.39.244
cat > /var/www/messager/frontend-web/.env.production << 'EOF'
VITE_TURN_USER=77f6dd0527ac33be2f81633f
VITE_TURN_CREDENTIAL=f8VrDZhPIgII74E6
VITE_TURN_SERVER=global.relay.metered.ca
EOF

cd /var/www/messager/frontend-web
npm run build
```

**После `npm run build` новая версия автоматически подхватится!**

---

## 📱 МОБИЛЬНЫЕ БРАУЗЕРЫ

Система также работает на мобильных:
- ✅ Chrome Mobile
- ✅ Safari iOS
- ✅ Firefox Mobile
- ✅ Samsung Internet

---

## 🎉 ИТОГ

### Теперь НЕ НУЖНО:
- ❌ Очищать кеш браузера вручную
- ❌ Использовать режим инкогнито
- ❌ Перезапускать браузер
- ❌ Ждать когда кеш устареет

### ДОСТАТОЧНО:
- ✅ Пересобрать: `npm run build`
- ✅ Обновить страницу: `Ctrl+R` или `F5`
- ✅ Новая версия загрузится автоматически!

---

## 🔍 ДИАГНОСТИКА

### Если всё равно грузится старая версия:

1. **Проверьте заголовки ответа (F12 → Network → index.html):**
   ```
   Cache-Control: no-cache, no-store, must-revalidate
   ```

2. **Проверьте хеш файла в index.html:**
   ```bash
   ssh root@89.169.39.244 "cat /var/www/messager/frontend-web/dist/index.html | grep index-"
   ```

3. **Проверьте время модификации файлов:**
   ```bash
   ssh root@89.169.39.244 "ls -lh /var/www/messager/frontend-web/dist/assets/index-*.js"
   ```

4. **Перезагрузите Nginx:**
   ```bash
   ssh root@89.169.39.244 "nginx -s reload"
   ```

---

## ⚡ БЫСТРЫЕ КОМАНДЫ

### Полный деплой с гарантией обновления:
```bash
ssh root@89.169.39.244 "cd /var/www/messager && git pull && cd frontend-web && npm run build && nginx -s reload && ls -lh dist/assets/index-*.js"
```

### Проверка текущей версии:
```bash
ssh root@89.169.39.244 "cat /var/www/messager/frontend-web/dist/index.html | grep -o 'index-[^.]*\.js'"
```

---

## 📝 CHANGELOG

### 31.01.2026 - Система версионирования внедрена
- ✅ Добавлены meta-теги в index.html
- ✅ Усилены заголовки Nginx
- ✅ Настроено автоматическое хеширование
- ✅ Создана документация

**Теперь обновления применяются мгновенно без проблем с кешем!** 🎉
