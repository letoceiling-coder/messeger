# ✅ Настройка TURN завершена (31.01.2026)

## 📋 Что было сделано

### 1. ✅ Проверили состояние серверов
- **Собственный coturn:** Работает (active running 1d 5h)
- **Порты:** 3478 UDP/TCP открыты и слушают
- **Файрвол:** Отключен (порты доступны)

### 2. ✅ Сохранили текущие настройки
- Создан бэкап облачного TURN (Metered.ca): `/var/www/messager/frontend-web/.env.production.metered.backup`

### 3. ✅ Обновили код `webrtc.service.ts`
- Убрали жёсткую привязку к Metered.ca
- Добавили поддержку переменной `VITE_TURN_SERVER`
- Теперь можно легко переключаться между серверами

### 4. ✅ Настроили фронтенд на собственный TURN
```env
VITE_TURN_USER=turn_fdf8b6e8
VITE_TURN_CREDENTIAL=U1cM4fhoxxqTnbb8XE9n
VITE_TURN_SERVER=89.169.39.244
```

### 5. ✅ Пересобрали фронтенд
Сборка успешна: `built in 3.28s`

---

## 🧪 Тестирование звонков

### Шаг 1: Очистить кэш браузера
1. Откройте мессенджер: `https://neekloai.ru` (или ваш домен)
2. Нажмите `Ctrl+Shift+Delete`
3. Очистите кэш и перезагрузите страницу (`Ctrl+F5`)

### Шаг 2: Попробовать разные варианты звонков

#### Вариант A: Звонок с ПК на ПК (та же сеть)
- Откройте мессенджер в двух браузерах/вкладках
- Войдите под разными пользователями
- Совершите видеозвонок
- **Ожидается:** Должно работать (даже без TURN через STUN)

#### Вариант B: Звонок ПК ↔ Телефон (разные сети) 🎯
**Это самый важный тест!**
- Откройте мессенджер на ПК и на телефоне
- Убедитесь, что телефон использует мобильный интернет (не Wi-Fi)
- Совершите видеозвонок
- **Ожидается:** Должно работать через ваш TURN сервер

#### Вариант C: Звонок через строгий NAT/Firewall
- Попробуйте из офисной сети с файрволом
- Или через мобильный интернет провайдера с NAT

### Шаг 3: Проверить логи во время звонка

**В интерфейсе звонка:**
1. Нажмите кнопку **"📋 Логи"**
2. Найдите строку: `ICE servers: Custom TURN 89.169.39.244 (4 servers)`
3. Проверьте состояние: `iceConnectionState: connected`

**Что смотреть:**
- ✅ `ICE servers: Custom TURN 89.169.39.244` — используется ваш сервер
- ✅ `iceConnectionState: connected` — соединение установлено
- ✅ `ICE gathering: complete` — все кандидаты собраны
- ❌ `iceConnectionState: failed` — соединение не установлено
- ❌ `Connection failed` — проблема с TURN

### Шаг 4: Проверить логи coturn на сервере

```bash
ssh root@89.169.39.244
tail -f /var/log/turnserver/turnserver.log
```

**Что должно появиться при звонке:**
```
session ...: new, user <turn_fdf8b6e8>, realm <neekloai.ru>
session ...: allocation created
session ...: usage: rp=..., rb=..., sp=..., sb=...
```

Это означает, что клиент подключился к вашему TURN и использует его для передачи данных.

---

## 🔄 Переключение обратно на Metered.ca (если нужно)

### Если собственный TURN не работает:

```bash
ssh root@89.169.39.244

# Восстановить настройки Metered.ca
cp /var/www/messager/frontend-web/.env.production.metered.backup /var/www/messager/frontend-web/.env.production

# НО! Нужно также вернуть старый код webrtc.service.ts
# Потому что новый код не знает про специфические адреса Metered.ca
```

**Или вручную:**
```bash
cat > /var/www/messager/frontend-web/.env.production << 'EOF'
VITE_TURN_USER=77f6dd0527ac33be2f81633f
VITE_TURN_CREDENTIAL=f8VrDZhPIgII74E6
VITE_TURN_SERVER=global.relay.metered.ca
EOF

cd /var/www/messager/frontend-web
npm run build
```

⚠️ **ВАЖНО:** С новым кодом Metered.ca будет использоваться только через порт 3478, а не 80/443 как раньше. Если нужна полная поддержка Metered.ca - верните старый код из `TURN_SETTINGS_BACKUP.md`.

---

## 📊 Сравнение конфигураций

### 🔵 Собственный TURN (ТЕКУЩАЯ)
```
Адрес: 89.169.39.244
Порты: 3478, 5349
Логин: turn_fdf8b6e8
Пароль: U1cM4fhoxxqTnbb8XE9n
Стоимость: 0 руб (ваш VPS)
Ограничения: Зависит от вашего сервера
```

### 🌐 Metered.ca (РЕЗЕРВ)
```
Адрес: global.relay.metered.ca
Порты: 80, 443
Логин: 77f6dd0527ac33be2f81633f
Пароль: f8VrDZhPIgII74E6
Стоимость: 0 руб (50 GB/месяц бесплатно)
Ограничения: 50 GB/месяц
```

---

## 🐛 Диагностика проблем

### Проблема: "Звонок не устанавливается"

**1. Проверить логи WebRTC:**
- Нажать "📋 Логи" в интерфейсе звонка
- Найти `ICE servers:` — должно быть `Custom TURN 89.169.39.244`
- Проверить `iceConnectionState` — должен дойти до `connected`

**2. Проверить порт 3478 снаружи:**
```bash
# С вашего компьютера
Test-NetConnection -ComputerName 89.169.39.244 -Port 3478
```

Должно быть: `TcpTestSucceeded : True`

**3. Проверить статус coturn:**
```bash
ssh root@89.169.39.244 "systemctl status coturn"
```

**4. Проверить логи coturn:**
```bash
ssh root@89.169.39.244 "tail -100 /var/log/turnserver/turnserver.log"
```

**5. Проверить настройки фронтенда:**
```bash
ssh root@89.169.39.244 "cat /var/www/messager/frontend-web/.env.production"
```

### Проблема: "Ошибка аутентификации"

Проверить, что логин и пароль в `.env.production` совпадают с `/etc/turnserver.conf`:
```bash
ssh root@89.169.39.244 "grep '^user=' /etc/turnserver.conf"
# Должно быть: user=turn_fdf8b6e8:U1cM4fhoxxqTnbb8XE9n
```

### Проблема: "ICE failed"

1. Проверить external-ip в конфиге:
```bash
ssh root@89.169.39.244 "grep '^external-ip=' /etc/turnserver.conf"
# Должно быть: external-ip=89.169.39.244
```

2. Проверить, что порт доступен снаружи
3. Проверить логи coturn на ошибки

---

## 📞 Контакты и доступы

**SSH:**
```
Сервер: 89.169.39.244
Пользователь: root
Пароль: r4w*F+jVbD2Z
```

**Быстрые команды:**
```bash
# Статус TURN
ssh root@89.169.39.244 "systemctl status coturn"

# Логи TURN
ssh root@89.169.39.244 "tail -f /var/log/turnserver/turnserver.log"

# Пересобрать фронтенд
ssh root@89.169.39.244 "cd /var/www/messager/frontend-web && npm run build"
```

---

## ✅ Чеклист готовности

- [x] coturn установлен и запущен
- [x] Порты 3478, 5349 открыты
- [x] Учётные данные настроены
- [x] Код webrtc.service.ts обновлён
- [x] Фронтенд пересобран
- [x] Бэкап Metered.ca создан
- [ ] **Тесты звонков выполнены** ← ВАША ЗАДАЧА
- [ ] **Работоспособность подтверждена** ← ВАША ЗАДАЧА

---

## 🎯 Следующие шаги

1. **Протестируйте звонки** по инструкции выше
2. **Проверьте логи** во время звонка
3. **Сообщите результаты:**
   - ✅ Работает — отлично!
   - ❌ Не работает — посмотрим логи и исправим
   - 🔄 Вернуться к Metered.ca — восстановим из бэкапа

После тестирования я помогу:
- Проанализировать логи если что-то не так
- Настроить дополнительно если нужно
- Вернуть Metered.ca если собственный TURN не подойдёт
