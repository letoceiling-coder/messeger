# ПОЧЕМУ ДВЕ ДИРЕКТОРИИ: ~/messager И public_html

## 🎯 КРАТКИЙ ОТВЕТ

**`~/messager/`** - это **рабочая директория** с исходным кодом для разработки и сборки.

**`~/parser-auto.site-access.ru/public_html/`** - это **публичная директория** для веб-сервера, куда размещается только **собранный Frontend**.

---

## 📁 ДЕТАЛЬНОЕ ОБЪЯСНЕНИЕ

### Структура на сервере:

```
/home/dsc23ytp/
├── messager/                          # Рабочая директория
│   ├── backend/                       # Backend исходники
│   │   ├── src/                       # Исходный код
│   │   ├── package.json
│   │   └── ...
│   ├── frontend-web/                  # Frontend исходники
│   │   ├── src/                       # Исходный код React
│   │   ├── package.json
│   │   └── ...
│   └── mobile/                        # Mobile исходники
│
└── parser-auto.site-access.ru/
    └── public_html/                   # Публичная директория
        ├── index.html                  # Собранный Frontend
        ├── assets/
        └── ...
```

---

## 🔧 КАК ЭТО РАБОТАЕТ

### 1. Backend (`~/messager/backend/`)

**Почему не в public_html?**
- Backend - это **Node.js приложение**, а не статические файлы
- Backend работает как **сервис** на порту 3000
- Backend **не должен быть доступен напрямую** через веб-сервер
- Backend обрабатывается через **PM2** или **systemd**

**Процесс:**
```bash
cd ~/messager/backend
npm install --production
npm run build          # Собирает в dist/
pm2 start ecosystem.config.js  # Запускает на порту 3000
```

**Доступ:**
- Внутренний: `http://localhost:3000`
- Внешний: `https://parser-auto.siteaccess.ru/api` (через Nginx proxy)

---

### 2. Frontend (`~/messager/frontend-web/` → `public_html/`)

**Исходники** (`~/messager/frontend-web/`):
- Исходный код React приложения
- Нужен для **сборки** (build)
- **Не должен быть доступен** напрямую через веб

**Build** (`~/parser-auto.site-access.ru/public_html/`):
- **Собранные файлы** после `npm run build`
- Статические файлы (HTML, CSS, JS)
- Доступны **напрямую** через веб-сервер
- Домен `https://parser-auto.siteaccess.ru/` указывает на эту папку

**Процесс:**
```bash
cd ~/messager/frontend-web
npm install
npm run build          # Собирает в dist/
cp -r dist/* ~/parser-auto.site-access.ru/public_html/  # Копирует build
```

**Доступ:**
- `https://parser-auto.siteaccess.ru/` → показывает файлы из `public_html/`

---

## 🌐 КОНФИГУРАЦИЯ NGINX

Nginx проксирует запросы:

```nginx
# Frontend (статические файлы)
https://parser-auto.siteaccess.ru/
  → /home/dsc23ytp/parser-auto.site-access.ru/public_html/

# Backend API (через proxy)
https://parser-auto.siteaccess.ru/api
  → http://localhost:3000/api

# WebSocket
https://parser-auto.siteaccess.ru/socket.io/
  → http://localhost:3000/socket.io/
```

---

## ✅ ПРЕИМУЩЕСТВА ТАКОЙ СТРУКТУРЫ

1. **Безопасность:**
   - Backend исходники не доступны через веб
   - Только собранные статические файлы в public_html

2. **Организация:**
   - Исходники отдельно от build
   - Легко обновлять и собирать
   - Можно иметь несколько версий build

3. **Гибкость:**
   - Легко откатывать изменения
   - Можно тестировать разные версии
   - Удобно для CI/CD

4. **Производительность:**
   - Статические файлы отдаются напрямую
   - Backend работает отдельно
   - Легко масштабировать

---

## 📋 СТАНДАРТНАЯ ПРАКТИКА

Это **стандартная структура** для веб-приложений:

- **Исходники** - в рабочей директории
- **Build** - в публичной директории веб-сервера
- **Backend** - работает как сервис, не через веб-сервер

Так делают все современные веб-приложения! ✅

---

## 🔄 ПРОЦЕСС РАЗВЕРТЫВАНИЯ

1. **Синхронизация исходников:**
   ```bash
   # Исходники → ~/messager/
   ```

2. **Сборка Backend:**
   ```bash
   cd ~/messager/backend
   npm run build
   pm2 restart messager-backend
   ```

3. **Сборка Frontend:**
   ```bash
   cd ~/messager/frontend-web
   npm run build
   cp -r dist/* ~/parser-auto.site-access.ru/public_html/
   ```

4. **Готово!**
   - Frontend доступен: `https://parser-auto.siteaccess.ru/`
   - Backend доступен: `https://parser-auto.siteaccess.ru/api`

---

## 💡 ИТОГО

- **`~/messager/`** - для разработки и сборки
- **`public_html/`** - для публичного доступа к Frontend
- **Backend** - работает как сервис на порту 3000
- **Frontend** - статические файлы в public_html

Это правильная и безопасная структура! ✅
