# 🔧 РУЧНАЯ НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

**После установки Java и Android Studio**

---

## 🔍 ШАГ 1: НАЙТИ ПУТИ УСТАНОВКИ

### Java JDK:

Откройте Проводник и проверьте эти папки:

```
C:\Program Files\Eclipse Adoptium\
C:\Program Files\Java\
C:\Program Files (x86)\Java\
```

**Ищите папку типа:** `jdk-17.0.17+10-hotspot` или подобную

**Запомните полный путь!** Например:
```
C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot
```

### Android SDK:

Откройте Android Studio и посмотрите путь к SDK:
1. Откройте Android Studio
2. Нажмите **"More Actions"** → **"SDK Manager"**
3. Вверху будет показан путь **"Android SDK Location"**

**Обычно это:**
```
C:\Users\dsc-2\AppData\Local\Android\Sdk
```

**Или:**
```
C:\Android\Sdk
```

**Запомните этот путь!**

---

## 🌍 ШАГ 2: НАСТРОИТЬ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

### Через GUI (проще):

1. Нажмите **Win + R**
2. Введите: `sysdm.cpl` → Enter
3. Вкладка **"Дополнительно"**
4. Нажмите **"Переменные среды"**

### Настроить JAVA_HOME:

1. В разделе **"Переменные пользователя"** нажмите **"Создать"**
2. **Имя переменной:** `JAVA_HOME`
3. **Значение:** Ваш путь к JDK (например `C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot`)
4. **ОК**

### Настроить ANDROID_HOME:

1. Нажмите **"Создать"** еще раз
2. **Имя переменной:** `ANDROID_HOME`
3. **Значение:** Ваш путь к Android SDK (например `C:\Users\dsc-2\AppData\Local\Android\Sdk`)
4. **ОК**

### Добавить в PATH:

1. Найдите переменную **`Path`** в **"Переменные пользователя"**
2. Нажмите **"Изменить"**
3. Нажмите **"Создать"** и добавьте по одной строке:

```
C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot\bin
C:\Users\dsc-2\AppData\Local\Android\Sdk\platform-tools
C:\Users\dsc-2\AppData\Local\Android\Sdk\tools
C:\Users\dsc-2\AppData\Local\Android\Sdk\tools\bin
```

**Замените пути на свои!**

4. **ОК** → **ОК** → **ОК**

---

## ⚡ ШАГ 3: ПЕРЕЗАПУСТИТЬ POWERSHELL

**ВАЖНО:** Закройте ВСЕ окна PowerShell и откройте новое!

---

## ✅ ШАГ 4: ПРОВЕРИТЬ

В **новом** окне PowerShell:

```powershell
java -version
echo $env:JAVA_HOME
echo $env:ANDROID_HOME
```

Должно показать:

```
openjdk version "17.0.17"
C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot
C:\Users\dsc-2\AppData\Local\Android\Sdk
```

---

## 🚀 ЕСЛИ ВСЁ РАБОТАЕТ:

Продолжайте сборку APK:

```powershell
cd c:\OSPanel\domains\Messager
.\scripts\build-apk.ps1 -Upload
```

---

**Настройте переменные и проверьте! 🔧**
