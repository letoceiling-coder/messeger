# СТРУКТУРА РАЗМЕЩЕНИЯ ПРОЕКТА НА СЕРВЕРЕ

## 📁 ПОЧЕМУ ДВЕ ДИРЕКТОРИИ?

### Структура на сервере:

```
~/messager/                    # Исходный код проекта
├── backend/                   # Backend (NestJS)
├── frontend-web/              # Frontend исходники
├── mobile/                    # Mobile исходники
└── nginx/                     # Конфигурации Nginx

~/parser-auto.site-access.ru/
└── public_html/               # Frontend BUILD (готовые файлы для веб-сервера)
    ├── index.html
    ├── assets/
    └── ...
```

---

## 🎯 ЗАЧЕМ ТАКАЯ СТРУКТУРА?

### 1. Backend (`~/messager/backend/`)

**Почему не в public_html?**
- Backend - это **Node.js приложение**, а не статические файлы
- Backend должен работать на **порту 3000** (или другом)
- Backend обрабатывается через **PM2** или **systemd**
- Backend **не должен быть доступен напрямую** через веб-сервер

**Как работает:**
- Backend запускается: `pm2 start ecosystem.config.js`
- Работает на: `http://localhost:3000`
- Доступен через Nginx proxy: `https://parser-auto.siteaccess.ru/api`

---

### 2. Frontend (`~/messager/frontend-web/` и `~/parser-auto.site-access.ru/public_html/`)

**Исходники** (`~/messager/frontend-web/`):
- Исходный код React приложения
- Нужен для сборки (build)
- Не должен быть доступен напрямую

**Build** (`~/parser-auto.site-access.ru/public_html/`):
- Собранные файлы после `npm run build`
- Статические файлы (HTML, CSS, JS)
- Доступны напрямую через веб-сервер
- Домен `https://parser-auto.siteaccess.ru/` указывает на эту папку

**Процесс:**
1. Редактируете исходники в `~/messager/frontend-web/`
2. Собираете: `npm run build`
3. Копируете build в `~/parser-auto.site-access.ru/public_html/`
4. Файлы доступны по домену

---

## 🔧 КАК ЭТО РАБОТАЕТ

### Backend

```
~/messager/backend/
├── src/              # Исходный код
├── dist/             # Собранный код (после npm run build)
└── ...

Запуск:
cd ~/messager/backend
npm run build
pm2 start ecosystem.config.js

Работает на: http://localhost:3000
```

### Frontend

```
Исходники: ~/messager/frontend-web/
Build:     ~/parser-auto.site-access.ru/public_html/

Процесс:
cd ~/messager/frontend-web
npm run build
cp -r dist/* ~/parser-auto.site-access.ru/public_html/

Доступен по: https://parser-auto.siteaccess.ru/
```

---

## 🌐 КОНФИГУРАЦИЯ NGINX

Nginx проксирует запросы:

```
https://parser-auto.siteaccess.ru/          → ~/parser-auto.site-access.ru/public_html/
https://parser-auto.siteaccess.ru/api/      → http://localhost:3000/ (Backend)
```

---

## ✅ ПРЕИМУЩЕСТВА ТАКОЙ СТРУКТУРЫ

1. **Безопасность:**
   - Backend не доступен напрямую
   - Только статические файлы в public_html

2. **Организация:**
   - Исходники отдельно от build
   - Легко обновлять и собирать

3. **Гибкость:**
   - Можно иметь несколько build'ов
   - Легко откатывать изменения

---

## 📋 ИТОГО

- **`~/messager/`** - рабочая директория с исходниками
- **`~/parser-auto.site-access.ru/public_html/`** - публичная директория для веб-сервера
- Backend работает отдельно на порту 3000
- Frontend build размещается в public_html для доступа по домену

Это стандартная структура для веб-приложений! ✅
