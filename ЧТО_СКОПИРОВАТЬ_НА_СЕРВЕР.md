# 📋 ЧТО КОПИРОВАТЬ НА СЕРВЕР

## ✅ СКОПИРУЙТЕ ВСЁ, КРОМЕ:

### Автоматически исключите (через WinSCP):

1. **`node_modules/`** - будут установлены через `npm install`
2. **`.git/`** - репозиторий Git
3. **`dist/`** в backend - будет собран через `npm run build`
4. **`logs/`** - логи
5. **`coverage/`** - отчеты покрытия тестами
6. **`.vscode/`, `.idea/`** - настройки IDE
7. **`mobile/`** - мобильное приложение (не нужно на сервере)
8. **`.env`** файлы (кроме `.env.production`)

### Опционально (можно не копировать):

- **`*.md`** файлы (документация)
- **`scripts/`** (скрипты для разработки)
- **`nginx/`** (конфиги уже на сервере)

---

## 🚀 ИНСТРУКЦИЯ ДЛЯ WINSCP

### Шаг 1: Настройте исключения

1. Откройте WinSCP
2. Перейдите: **Settings → Preferences → Transfer → Exclude**
3. Добавьте исключения из файла `scripts/exclude-list.txt`

Или вручную добавьте:
```
node_modules
.git
dist
logs
coverage
.vscode
.idea
mobile
.env
.env.local
.env.development
```

### Шаг 2: Скопируйте проект

1. Подключитесь к `root@89.169.39.244`
2. Перейдите в `/var/www/messager/`
3. Скопируйте **всю папку проекта** из `C:\OSPanel\domains\Messager\`
4. WinSCP автоматически исключит ненужные файлы

### Шаг 3: Выполните на сервере

```bash
ssh root@89.169.39.244

# Backend
cd /var/www/messager/backend
npm install --production
npx prisma generate
npx prisma migrate deploy
npm run build
pm2 start ecosystem.config.js --update-env

# Frontend (если нужно собрать заново)
cd /var/www/messager/frontend-web
npm install --production
npm run build

# Сохранить PM2
pm2 save
```

---

## ✅ ГОТОВО!

После копирования и выполнения команд проект будет работать!
