# 🚀 УСТАНОВКА ПО НА VPS - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ⚠️ ПРОБЛЕМА С SSH

Если не получается подключиться по SSH, используйте **веб-терминал в панели Beget**.

---

## 🎯 ШАГ 1: ОТКРЫТЬ ВЕБ-ТЕРМИНАЛ

1. Зайдите в панель Beget: `https://cp.beget.com`
2. Откройте раздел VPS (сервер "Instinctive Pavel")
3. Найдите раздел **"Терминал"** (в боковом меню или карточка на странице)
4. Нажмите на него
5. Откроется веб-терминал

---

## 📋 ШАГ 2: ВЫПОЛНИТЬ СКРИПТ УСТАНОВКИ

### Вариант 1: Скопировать и выполнить команды по одной

Скопируйте и выполните в веб-терминале:

```bash
# Обновление системы
apt-get update
apt-get upgrade -y

# Установка Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Установка MySQL
apt-get install -y mysql-server
systemctl start mysql
systemctl enable mysql

# Установка Nginx
apt-get install -y nginx
systemctl start nginx
systemctl enable nginx

# Установка PM2
npm install -g pm2

# Установка дополнительных утилит
apt-get install -y git curl wget build-essential

# Создание директорий
mkdir -p /var/www/messager/backend
mkdir -p /var/www/messager/frontend-web
mkdir -p /var/www/messager/backend/uploads
mkdir -p /var/www/messager/backend/logs
```

### Вариант 2: Использовать готовый скрипт

1. Скопируйте содержимое файла `scripts/install-vps.sh`
2. Вставьте в веб-терминал
3. Нажмите Enter

---

## ✅ ШАГ 3: ПРОВЕРКА УСТАНОВКИ

После выполнения скрипта проверьте:

```bash
node --version
npm --version
mysql --version
nginx -v
pm2 --version
```

Все должно быть установлено!

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

После установки ПО я помогу:

1. **Настроить MySQL** (создать базу данных)
2. **Перенести проект** на VPS
3. **Настроить Nginx** для проксирования
4. **Запустить Backend**

---

## 🚀 ГОТОВО!

**Выполните скрипт установки в веб-терминале панели Beget!**

После установки сообщите мне, и я помогу с настройкой проекта.
