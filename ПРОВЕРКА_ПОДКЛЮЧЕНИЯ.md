# ✅ ПРОВЕРКА ПОДКЛЮЧЕНИЯ

## 🎉 SSH КЛЮЧ УСПЕШНО ДОБАВЛЕН!

Вы успешно выполнили все команды на сервере. Теперь проверим подключение.

---

## ШАГ 1: ПРОВЕРИТЬ ПОДКЛЮЧЕНИЕ БЕЗ ПАРОЛЯ

### На вашем компьютере (Windows)

**PowerShell или Git Bash:**
```bash
ssh dsc23ytp@dragon
```

**Если подключилось БЕЗ ПАРОЛЯ** - отлично! ✅

**Если все еще требует пароль:**
- Проверьте права на сервере: `ssh dragon "ls -la ~/.ssh/"`
- Должно быть: `drwx------` для `.ssh` и `-rw-------` для `authorized_keys`
- Если нет, выполните на сервере:
  ```bash
  chmod 700 ~/.ssh
  chmod 600 ~/.ssh/authorized_keys
  ```

---

## ШАГ 2: НАСТРОИТЬ SSH CONFIG НА WINDOWS

### Автоматически

```powershell
.\setup-ssh-simple.ps1
```

Введите `dragon` когда спросит адрес сервера.

### Вручную

1. Откройте файл: `C:\Users\ВашеИмя\.ssh\config`
2. Добавьте:
   ```
   Host dragon
       HostName dragon
       User dsc23ytp
       IdentityFile C:\Users\ВашеИмя\.ssh\id_rsa
   ```

После этого можно подключаться просто: `ssh dragon`

---

## ШАГ 3: ПРОВЕРИТЬ СТРУКТУРУ НА СЕРВЕРЕ

```bash
ssh dragon "ls -la ~/messager/"
```

Должны быть директории:
- `backend/`
- `frontend-web/`
- `mobile/`
- `nginx/`

---

## ШАГ 4: СИНХРОНИЗИРОВАТЬ ФАЙЛЫ

После проверки подключения:

**PowerShell:**
```powershell
.\scripts\sync-to-server.ps1
```

**Git Bash:**
```bash
./scripts/sync-to-server.sh
```

Это скопирует все файлы проекта на сервер.

---

## ✅ ВСЕ РАБОТАЕТ?

Если подключение работает без пароля, вы готовы к следующему шагу!

**Следующие шаги:**
- См. файл `СЛЕДУЮЩИЕ_ШАГИ.txt`
- Или `QUICK_DEPLOY_DRAGON.md` для полного развертывания

---

## 🆘 ЕСЛИ ПРОБЛЕМЫ

### Все еще требует пароль

1. Проверьте ключ на сервере:
   ```bash
   ssh dragon "cat ~/.ssh/authorized_keys"
   ```

2. Сравните с вашим ключом:
   ```powershell
   type $env:USERPROFILE\.ssh\id_rsa.pub
   ```

3. Если ключи не совпадают, добавьте ключ снова (см. `СКОПИРУЙТЕ_ЭТИ_КОМАНДЫ.txt`)

### Ошибка "Host key verification failed"

```bash
ssh-keygen -R dragon
```

Затем попробуйте подключиться снова.

---

## 💡 СОВЕТ

После успешной настройки SSH:
- Используйте VS Code Remote SSH для удобной работы
- Используйте скрипты для автоматической синхронизации
- Работайте напрямую с файлами на сервере
