# ✅ РЕШЕНИЕ ОШИБКИ 404 ДЛЯ API

## ⚠️ ПРОБЛЕМА

**Ошибка:** `404 Not Found` при попытке войти в систему

**Причина:** 
1. Backend не запущен или не доступен
2. Apache не проксирует запросы `/api` к Backend

---

## ✅ РЕШЕНИЕ

### 1. Запустить Backend

Backend должен быть запущен на порту 30000.

**Проверка:**
```bash
ssh dsc23ytp@5.101.156.207
curl http://localhost:30000/health
```

Должен вернуть: `{"status":"ok"}`

**Если не работает:**
```bash
cd ~/messager/backend
export JWT_SECRET='iIPvW1ifTAzaH7FOu2Q19o4sFSHlcLMekdOIFzmkanE='
export DATABASE_URL='mysql://dsc23ytp_mess:r7nCbBSN%cr3@localhost:3306/dsc23ytp_mess'
export NODE_ENV=production
export PORT=30000
export CORS_ORIGIN='https://parser-auto.siteaccess.ru'

nohup node dist/main.js > backend.log 2>&1 &
```

### 2. Создать пользователя

```bash
curl -X POST http://localhost:30000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "username": "admin",
    "password": "admin123"
  }'
```

### 3. Настроить проксирование Apache

**Проблема:** `ProxyPass` не работает в `.htaccess` - нужна основная конфигурация Apache.

**Решение:** Обратиться в поддержку Beget.

**Текст запроса:** см. файл `ЗАПРОС_В_ПОДДЕРЖКУ_BEGET.txt`

---

## 🔧 ВРЕМЕННОЕ РЕШЕНИЕ

Пока проксирование не настроено, можно:

1. **Использовать прямое подключение к Backend** (только для тестирования)
2. **Или настроить поддомен** для API через панель Beget

---

## 📋 СТАТУС

- ✅ Frontend работает
- ✅ Страница логина открывается
- ⚠️ Backend нужно запустить
- ⚠️ API не проксируется (нужна настройка через поддержку)

---

## ✅ ГОТОВО!

После запуска Backend и настройки проксирования все будет работать!
