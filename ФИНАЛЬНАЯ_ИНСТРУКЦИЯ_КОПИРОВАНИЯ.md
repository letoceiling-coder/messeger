# 🚀 ФИНАЛЬНАЯ ИНСТРУКЦИЯ ПО КОПИРОВАНИЮ ПРОЕКТА

## ✅ ВСЁ ГОТОВО К КОПИРОВАНИЮ!

Я очистил папку `/var/www/messager/` на сервере. Теперь скопируйте проект.

---

## 📋 ШАГ 1: НАСТРОЙТЕ WINSCP

### Добавьте исключения:

1. Откройте WinSCP
2. **Settings → Preferences → Transfer → Exclude**
3. Нажмите **Add** и добавьте по одной строке:

```
node_modules
.git
dist
logs
coverage
.vscode
.idea
mobile
*.md
!README.md
backend.tar.gz
```

4. Нажмите **OK**

---

## 📋 ШАГ 2: ПОДКЛЮЧИТЕСЬ К СЕРВЕРУ

- **Хост:** `89.169.39.244`
- **Пользователь:** `root`
- **Пароль:** `r4w*F+jVbD2Z`
- **Протокол:** SFTP

---

## 📋 ШАГ 3: СКОПИРУЙТЕ ПРОЕКТ

1. В WinSCP перейдите в `/var/www/messager/`
2. Слева (локальный компьютер): `C:\OSPanel\domains\Messager\`
3. Скопируйте **всю папку проекта** (перетащите или Ctrl+C/Ctrl+V)
4. WinSCP автоматически исключит ненужные файлы!

**Или скопируйте отдельно:**
- `backend/` → `/var/www/messager/backend/`
- `frontend-web/` → `/var/www/messager/frontend-web/`

---

## 📋 ШАГ 4: ВЫПОЛНИТЕ КОМАНДЫ НА СЕРВЕРЕ

После копирования подключитесь через SSH:

```bash
ssh root@89.169.39.244
```

### Backend:

```bash
cd /var/www/messager/backend

# Установить зависимости
npm install --production

# Сгенерировать Prisma Client
npx prisma generate

# Применить миграции
npx prisma migrate deploy

# Собрать проект
npm run build

# Запустить через PM2
pm2 start ecosystem.config.js --update-env

# Сохранить PM2 конфигурацию
pm2 save
```

### Frontend (если скопировали исходники, а не dist):

```bash
cd /var/www/messager/frontend-web

# Установить зависимости
npm install --production

# Собрать проект
npm run build
```

---

## ✅ ПРОВЕРКА

```bash
# Проверить Backend
curl http://localhost:3000/health

# Проверить PM2
pm2 status

# Проверить логи
pm2 logs messager-backend --lines 20
```

---

## 🔑 ДАННЫЕ ДЛЯ ВХОДА

После развертывания:
- **Email:** `admin@example.com`
- **Password:** `admin123`

**URL:** `http://89.169.39.244`

---

## 🎉 ГОТОВО!

После выполнения всех шагов проект будет работать!
